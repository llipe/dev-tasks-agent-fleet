# Technical Guidelines — Agent Fleet Control Plane

## Changelog

| Version | Date       | Summary                                                                 | Author           |
| ------- | ---------- | ----------------------------------------------------------------------- | ---------------- |
| 1.0     | 2026-08-26 | Initial version. Reformatted from consolidated PRD, `001_schema.sql`, `002_seed.sql`, `agent_reporter.py`, and `credentials.ts` into foundation doc format. No scope or decision changes. | product-engineer |
| 1.1     | 2026-08-26 | Translated to English. Aligned with two-phase delivery model. | product-engineer |
| 1.2     | 2026-08-27 | Documented the implemented LLM fix-agent escape hatch (issue #75): added the sandbox + mandate-backstop security rule to §6, replaced the stale §11 Testing Strategy with the implemented pytest layer taxonomy, and recorded Strands/Bedrock as a committed agent runtime dependency in §16. See [ADR-001](adr/ADR-001-llm-fix-agent-escape-hatch.md). | technical-writer |
| 1.3     | 2026-08-27 | Documented the implemented `open_pr` step + `pull_request` run artifact (issue #76): updated the §9 `dependency-update` status line (PR-creation/artifact no longer deferred; only `runs.metrics` persistence + deploy/E2E remain, → #77) and refreshed the §11 test surface (PR-creation Layer 1 + Layer 2 tests; suite now 328 passing). Current-state status correction only — no new architectural decision or enforceable-rule change. See [ADR-002](adr/ADR-002-open-pr-step-and-pr-artifact.md). | technical-writer |
| 1.4     | 2026-08-27 | Documented the run-metric under-reporting fix (issue #90): refreshed the §9 `dependency-update` status line to record that `advisories_fixed` is now an audit ID-set diff and `packages_changed` a workspace-aware recursive lockfile snapshot (with a single fixed-advisory count feeding both the PR body Security Summary and `runs.metrics`), and updated the §11 test surface + suite count (now 362 passing). Current-state status correction only — no new architectural decision or enforceable-rule change. See [ADR-003](adr/ADR-003-run-metric-under-report-fix.md). | technical-writer |
| 1.5     | 2026-08-31 | Recorded the `pg_cron` reaper activation and its verification (issue #94): added §18 "Open defects discovered during reaper verification" registering #97/#98/#99/#100, the empirically verified reaper properties (fires 12.3 s after the 3720 s threshold, writes the explanatory event at `max(seq)+1`, two-layer convergence confirmed), and the accepted ~61-minute stale window from the D8 threshold choice. See [ADR-004](adr/ADR-004-schedule-pg-cron-reaper.md). | developer |
| 1.6     | 2026-08-31 | Documentation-gate pass for issue #94 (PR #96) under the same decision record, [ADR-004](adr/ADR-004-schedule-pg-cron-reaper.md) — no new decision taken. Corrected the sections written while the reaper was still unscheduled: §2 and §3 now state the job is registered `* * * * *` and empirically confirmed, §7 records the current scheduling state plus the `RUNTIME_TIMEOUT`/`START_TIMEOUT` event contract and the #99 orphan-step caveat, §14 adds the reaper observability surface (`cron.job_run_details` + the explanatory event), and §18 records the residual verification carried by #101 (AC5, AC6, AC4 `queued` half) and marks the 185.7 s cold-start figure invalid so it is not cited as a measurement. | technical-writer |
| 1.7     | 2026-09-01 | Documented the repeated `prompt`-wrapper unwrap + distinct double-wrap diagnostic (issue #97, PR #102): added a §8 "Invocation payload contract" subsection recording that `unwrap_payload` now unwraps lone-`prompt` wrappers repeatedly (bounded by `_MAX_UNWRAP_DEPTH=16`, guarded by `_is_lone_prompt_wrapper`) to tolerate the `agentcore` CLI ≥0.28.0 double-wrap, and that a still-wrapper-only payload emits an "appears double-wrapped" diagnostic via `classify_invalid_payload` while `error_code` stays `INVALID_PARAMS` (no new error code, no schema change, no migration); flipped the §18 #97 row from Open to Resolved. Current-state status correction only — no enforceable-rule change. See [ADR-005](adr/ADR-005-repeated-prompt-unwrap-and-diagnostic.md). | technical-writer |
| 1.8     | 2026-09-01 | Documented the `validate`-step keep-alive fix (issue #98, PR #103): added a §8 subsection recording the heartbeat keep-alive (`heartbeat.run_with_heartbeat` live-yields chunks during `validate`/`llm_fix`), the four-clock consistency invariant enforced by `config.assert_clock_invariant()` (`TOOL_COMMAND_TIMEOUT ≤ TEST_TIMEOUT ≤ IDLE_SESSION_TIMEOUT ≤ MAX_LIFETIME ≤ REAPER_THRESHOLD_SECONDS`, heartbeat ≤ idle/2), the `idleRuntimeSessionTimeout` 300 → 900 change (requires redeploy), and the best-effort SIGTERM backstop (`signal_backstop.py`; SIGKILL/OOM stays reaper-only); flipped the §18 #98 row to Resolved (code) with live AC2/AC3 verification pending. Introduces an enforceable clock-consistency rule (fail-fast at startup) plus the heartbeat keep-alive and SIGTERM-backstop mechanisms — recorded as a decision in [ADR-006](adr/ADR-006-long-step-keepalive-and-clock-invariant.md). | developer / technical-writer |
| 1.9     | 2026-09-01 | Documented the reaper orphan-`run_steps` fix (issue #99): `reap_stale_runs()` now closes any open `run_steps` (`status='failed'`, `finished_at=now()`, attributing `error_message`) on **both** its branches (`timed_out` and `failed_to_start`), in symmetry with the agent failure path (§8). Refreshed §7 (orphan-step caveat reworded from open-defect to resolved; one stale-window caveat remains), added the §8 "Reaper mirrors the agent's step-closure" note, and flipped the §18 #99 row Open → Resolved. Reuses the existing `step_status` enum value `failed` (no new enum value, no migration); the DDL change lives in `docs/reference/001_schema.sql` and is applied via `create or replace function`. | developer |
| 1.10    | 2026-09-01 | Documented the insert-then-invoke contract + defensive `start()` guard (issue #100): added the §8 behavioral-property row recording that `RunReporter.start()` now issues its `running` PATCH with `Prefer: count=exact`, reads `Content-Range`, and on a **confirmed** zero-row match (no `queued` row inserted — D1) logs a loud stderr warning naming #100 and continues (reporting never kills the agent; an unknown count does not warn). The change is mirrored byte-identically into both `agent_reporter.py` copies (D13). Agent-doc changes (README §Invocation prerequisite + silent-no-op diagnosis) are current-state only; the SDK guard is a small behavioral addition under the existing D1/D15 reporting-contract context, no new enforceable rule. | developer |
| 1.11    | 2026-09-02 | Documented the Phase 2 JS/TS kickoff (issue #114 / S-101): the previously-deferred front-end sections now describe the shipped reality. §12 replaced "no `package.json` at root" with the real pnpm workspace + `panel` ESLint/Prettier/TypeScript-strict toolchain and canonical scripts; §9 records the pnpm workspace, the `panel/` Next.js App Router package, and the two-branch `make validate`; §16 pins the `panel` render/validation dependency tree (`next` 15.5.25 backport line off the RCE-carrying 15.5.4, `react` 19.1.1, `ajv` 8.17.1, Vitest 3.2.4, Playwright 1.56.0; root `pnpm.overrides` for postcss/sharp; one sub-high `ajv` advisory noted; `@aws-sdk/*` + `@supabase/supabase-js` deferred to S-104/S-105/S-111); §11 flips the front-end test line from "no JS/TS test package yet" to the Vitest projects + Playwright stub; §13 notes the panel scaffold exists but is not yet deployed; §1/§2 Phase-2 markers refined to "scaffold exists, not yet deployed". Current-state status correction only — the pnpm/canonical-scripts standard, SD2 server-only Supabase, and the F7 aggregate-gate rule all trace to existing decisions (spec/PRD + §11/`TESTING.md`), so no new enforceable guideline changed and no ADR is required (precedent: rows 1.3/1.4/1.6/1.7). The `next` 15.5.4→15.5.x spec pin write-back is routed to `product-engineer`, not made here. | technical-writer |
| 1.12    | 2026-09-02 | Documented Supabase CLI migration adoption (issue #115 / S-102, PR #130): the canonical schema/seed moved to `supabase/migrations/20260902200101_initial_schema.sql` (byte-identical to the former reference DDL, incl. `pg_cron` + the `reap-stale-runs` schedule) and `supabase/seed.sql`, applied by `supabase db reset`; `supabase/config.toml` added. §7 header rewritten to name the migration + seed as canonical and `docs/reference/001_schema.sql`/`002_seed.sql` as non-canonical zero-DDL pointer stubs; §7 reaper current-state prose re-pointed from `001_schema.sql` to the migration; §8 GitHub App manual-seed link re-pointed to `supabase/seed.sql`; §9 artifact table split into canonical (migration/seed/config.toml) and stub rows. Current-state status correction only — migration adoption is the SD3 decision already recorded in the spec/story, no new architectural decision, so no ADR is required (precedent: rows 1.3/1.4/1.6/1.7/1.11). Layer 2.5 integration-harness/`TESTING.md`/`panel/README.md` changes were already made on-branch and are the `qa-engineer`/implementer surface, not re-authored here. PRD Phase-1 reference-file links left as historical requirements text. | technical-writer |
| 1.13    | 2026-09-03 | Documentation-gate pass for the English-only SQL surface (issue #116 / S-103, PR #131): reviewed and confirmed the §8 `start_timeout_seconds`-is-a-queue-clock note (direction A — corrected the false `= idleRuntimeSessionTimeout` equivalence in `supabase/seed.sql` block 3, kept the value at 300; queue clock per D9, distinct from the output-idle clock raised 300→900 in #98) as correct and consistent with §7/§8; no value and no change to the four-clock invariant. S-103 shipped two behavior-preserving migrations (`20260903090000_english_reaper_messages.sql` translating the reaper's `error_message`/`run_events` text to English on both branches with error_code/`seq=max+1`/`reaped_by`/`reason`/#99 step-closure all unchanged; `20260903090100_seed_params_schema_english.sql` translating `params_schema` title/description with additionalProperties/required/enum/default/min/max unchanged) plus `supabase/seed.sql` comment/label translation; all three applied to live project via `db push` (user-confirmed, Local + Remote). No enforceable-rule or decision change in this doc — current-state correction only (precedent rows 1.3/1.4/1.6/1.7/1.11/1.12), so no new ADR. Separately corrected the `TESTING.md` "Database / reaper layer" section: it claimed the reaper had "zero automated coverage" and lived only in `docs/reference/001_schema.sql` applied by hand — both now false. Credited the S-102 migration move and the S-103 Layer 2.5 suite (`panel/tests/integration/reaper.test.ts` 7 tests, `seed-schema.test.ts` 4 tests, `schema.test.ts` 3 tests baseline); reranked gap 4 closed, gaps 1/3 partly closed, gaps 2/5/6 remain open. | technical-writer |
| 1.14    | 2026-09-03 | Documented the panel's server-side Supabase read boundary + `effectiveStatus` SD4 mirror (issue #117 / S-104, PR #132): §9 `panel/` row now records the shipped `lib/supabase/{server,queries,types,errors,route-config}.ts` read layer and `lib/domain/status.ts` (no longer scaffold-only); §16 flips `@supabase/supabase-js` from "deferred to S-104" to pinned **`2.114.0`** (spec §16 named 2.58.0 — re-confirmed current + audit-clean, S-101 precedent) and adds `server-only@0.0.1`; §11 adds the S-104 Layer 1 (`status`, `errors`, `server-env`, `eslint-server-import`, `bundle-secrets`) + Layer 2.5 (`status-parity`, `rls-deny-all`, `queries`) test surface (68 panel tests + CT-7 bundle test); §7 records two current-state findings — the service_role SELECT grant asymmetry (canonical migration grants none; the LIVE project carries platform-default grants per the S-115 baseline diff, so production reads work but `supabase db reset` does not reproduce them and the queries integration test applies the grant locally, service_role only, RLS deny-all preserved — production read path proven by inference, not live assertion) and the PostgREST `max_rows=1000 < SD11 2000` internal `.range()` paging in `getRunEvents`; §12 records the inline route-segment-config convention (Next.js does not honor re-exported segment config). Current-state status correction only — the SD2 server-only boundary, no-`NEXT_PUBLIC`, RLS deny-all, and force-dynamic guarantees all trace to existing spec/PRD decisions (D11/D15/D16, SD2/SD4/SD11), no enforceable guideline changed, so no new ADR is required (precedent: rows 1.3/1.4/1.6/1.7/1.11/1.12/1.13). No schema/migration change shipped. The `@supabase/supabase-js` 2.58.0→2.114.0 spec write-back and the inferred-vs-live production-read note are routed to `product-engineer`, not made here. Separately corrected stale `panel/README.md` route-config guidance (re-export → inline). | technical-writer |
| 1.15    | 2026-09-04 | Documented the Nocturne design-token layer + the twelve §11.2 primitives (issue #118 / S-105, PR #133): §2 front-end row and §9 `panel/` row now record the shipped presentational + pure layer (`styles/tokens.css` with every DESIGN §2 token incl. the four SD10 `--st-*`, `styles/globals.css` keyframes/`:focus-visible`/reduced-motion, `lib/format.ts` §7 formatters, the twelve `components/*` primitives each with a token-only `.module.css`, `components/status-meta.ts`, `components/icons.tsx` (Phosphor `/ssr` on `currentColor`, §10), and the dev-only `app/dev/gallery/*` that 404s in production); §11 adds the S-105 Layer 1 (`format`, `token-discipline`) + Layer 2 component (12 suites) test surface with `coverage_gate` PASS (100% on every S-105 component + `lib/format.ts`, panel 232 passed/19 skipped, `make validate` exits 0) and notes the harness-only single-threaded `integration` project pin; §12 adds the Nocturne token-discipline convention (mechanical hex/`font-family` gate, documented bare-`px` carve-out, Phosphor icon rule, `color-mix()` evergreen-browser floor); §16 marks `@phosphor-icons/react` 2.1.10 as now actively used and corrects the stale `@aws-sdk/*` "deferred to S-105/S-111" reference to S-111 only (S-105 is the token/primitive layer, not the invocation path). Current-state status correction only — the Nocturne-tokens + CSS-Modules + token-discipline standard traces to DESIGN.md §11.1 / S-101 and the formatters/primitives/icons implement the existing DESIGN §2/§7/§10/§11.2 contract, so no new architectural decision and no new ADR is required (precedent: rows 1.3/1.4/1.6/1.7/1.11/1.12/1.13/1.14). No schema/data/API change. The verifier's DESIGN.md self-contradiction findings (D1 §3.4↔§8.1 pill tint, D2 queued pulse cadence, D3 §7.1 compact `14m ago`) are routed to `product-engineer` for DESIGN.md write-back — not touched here. | technical-writer |
| 1.16    | 2026-09-04 | Documented the panel's AWS credential + invocation path (issue #124 / S-111, PR #135): §5 re-points the Fly-auth branch link from `docs/reference/credentials.ts` to the now-canonical [`panel/lib/aws/credentials.ts`](../panel/lib/aws/credentials.ts), records the **F5 fix** (`extractOidcToken` accepts only string `value`/`token` and throws `FlyOidcShapeError` naming the received keys — the `parsed.aud` + raw-`data.trim()` fallbacks are removed so the audience is never forwarded to STS as a token), the `AGENT_RUNTIME_ROLE_ARN` fail-fast (renamed from the reference stub's `AWS_ROLE_ARN`), the §13/R6 `CREDENTIALS_UNAVAILABLE` 500 vs `INVOCATION_FAILED` 502 taxonomy, and clarifies that only the **live** Fly-socket probe / `sub` normalization / `MaxSessionDuration` compatibility remain deferred (to S-115 / OQ1); §9 artifact table replaces the `credentials.ts` provider row with a MOVED-stub row + a canonical `panel/lib/aws/{credentials,errors,invoke}.ts` row, and the `panel/` row now records the shipped `lib/aws/*` layer; §16 flips the four `@aws-sdk/*` packages from "deferred to S-111" to pinned (three clients `3.1126.0`, `@aws-sdk/types` `3.974.5`, single deduplicated `@smithy/core@3.33.3`, audit gate still green); §11 adds the S-111 Layer 1 test surface (36 tests across `credentials`/`aws-errors`/`invoke`, `coverage_gate` PASS `lib/aws` 97.44% stmts, `make validate` Python 436 + panel 275). Current-state status correction only — the two-branch provider, no-static-keys (D12), and the credential/invocation error taxonomy all trace to existing spec decisions (SD9, §9.1, §13, R6); the F5 change is a defect fix inside adopted reference code, not a new enforceable guideline, so no new ADR is required (precedent: rows 1.3/1.4/1.6/1.7/1.11/1.12/1.13/1.14/1.15). No schema/data change. The verifier's one Minor drift (D1 — the "@aws-sdk/* same minor" plan wording vs `@aws-sdk/types`' independent cadence) and the `AWS_ROLE_ARN`→`AGENT_RUNTIME_ROLE_ARN` spec note are routed to `product-engineer`, not written back here. | technical-writer |
| 1.17    | 2026-09-04 | Documented the Phase 2 panel **app shell** (issue #119 / S-106, PR #138): the shell is presentational + pure with no schema/data/API change. §2 front-end row and §9 `panel/` row now record `components/shell/{AppShell,Sidebar,TopBar,DisabledNavItem}.tsx` (212px/52px collapsible sidebar, 38px top bar with a breadcrumb slot + optional `actions` slot, content region owning its scroll under `100dvh`/`overflow:hidden` per DESIGN §4.1) and `lib/ui/{shortcuts,sidebar-state}.ts` (the Cmd+\ / Ctrl+\ toggle matcher + typing-target guard, and the closed `{collapsed,expanded}` localStorage vocabulary keyed `panel.sidebar.collapsed`, defaulting to expanded, that never throws); captured the **hydration contract** (fixed expanded server default reconciled from storage in a mount effect, never during render — the S-106 design decision) and the Agents-only-enabled / four-deferred-`DisabledNavItem` nav per PRD §10. §12 records the SD2 `no-restricted-imports` glob extension from `components/**` to also cover `app/**` (the S-104 audit D1 hardening), with App Router server entrypoints excluded so the rule still never fires where SD2 reads legitimately live. §11 adds the S-106 Layer 1 (`sidebar-state`, `shortcuts`) + Layer 2 (`AppShell` incl. the falsifiable G3 hydration instrument) test surface with `coverage_gate` PASS (100% on every S-106 module; panel 331 passed / 19 Docker-gated skips) and the accepted AC-106.6 1024px-geometry gap deferred to S-114 (verifier D4/G7). Two new derived surface tokens `--color-sidebar-bg` (92% bg over #000) and `--color-shell-bg` (88%) were added to `styles/tokens.css` so shell CSS references a token instead of a raw `#000` (token-discipline gate) — noted in the §9 shell entry. Current-state status correction only — the shell/CSS-Modules/Nocturne-token and SD2 server-only standards all trace to existing decisions (DESIGN §4.1/§6.5, PRD §10, SD2, S-101/S-104/S-105), so no enforceable guideline changed and no new ADR is required (precedent: rows 1.3/1.4/1.6/1.7/1.11/1.12/1.13/1.14/1.15/1.16). No schema/data/API change; no user-guide or OpenAPI parity triggered (neither exists yet — the panel is a pre-deploy scaffold). The verifier's Minor Intended drift findings (D1 file-layout, D2 top-bar `actions` slot, D3 a11y over-delivery, D4 AC-106.6 geometry, D5 dimensional `px` carve-out) are routed to `product-engineer` for optional PRD/story write-back, not made here. | technical-writer |
| 1.18    | 2026-09-04 | Documented the Phase 2 **agents dashboard** (issue #120 / S-107, PR #139): a read-only UI story with no schema/data/API change. §2 front-end row and §9 `panel/` row now record the dashboard at `/` — `app/page.tsx` (thin async server component declaring `force-dynamic`/`revalidate=0`/`fetchCache` **inline** per the S-104 audit D4 / §12 convention, since a cached run list is exactly the staleness FR11a prevents), `lib/domain/dashboard.ts` (the pure `buildAgentSummaries` shaper deriving **every** emitted status — `recentRuns`, `lastRun`, and every `breakdown`/`legend` count — through `effectiveStatus` per FR11a/CT-1, importing the SD4 derivation from `lib/domain/status.ts` and never restating thresholds CT-3; plus the `matchesQuery`/`filterAgentSummaries` literal-substring filter EC-25), `lib/ui/density-state.ts` (the closed `{dense,cards,ledger}` localStorage vocabulary keyed `panel.dashboard.density`, defaulting to `dense`, never throwing — same hydration contract as the sidebar), `components/dashboard/*` (the three DESIGN §5.1 variants DenseRows/AgentCards/Ledger, DensityToggle, the transient never-persisted AgentFilter, DashboardClient, summary-view), the new `getDashboardData` helper in `lib/supabase/queries.ts` (two reads never N+1 CT-7, paged below PostgREST `max_rows=1000` so counts stay true beyond 1000 CT-8), and a small `forwardRef` addition to `components/Input.tsx`. §11 adds the S-107 Layer 1 (`dashboard`, `density-state`, `summary-view`) + Layer 2 (`dashboard.tsx`) + Layer 2.5 (`dashboard-query`, ran live) test surface (69 non-integration tests) with `coverage_gate` PASS (100% on `dashboard.ts`/`density-state.ts`; `make validate` green — Python 436 + panel 428 passed / 24 Docker-gated skips locally). Recorded scope decision: the DESIGN §5.1 time-range chips (7d/30d/all) were **not** built ("all" only — a client window misreports counts, a server window reintroduces the forbidden toggle refetch). Current-state status correction only — the SD2 server-only boundary, SD4 `effectiveStatus`, inline route-config, and Nocturne token discipline all trace to existing decisions/ADRs, so no new architectural decision and no new ADR is required (precedent: rows 1.11/1.12/1.13/1.14/1.15/1.16/1.17). No schema/data/API change; no user-guide or OpenAPI parity triggered (neither exists yet — the panel is a pre-deploy scaffold). The verifier's Minor/Intended drift (D1 time-range chips → DESIGN §5.1 write-back, D2 1024px geometry → S-114) is routed to `product-engineer`, not made here. | technical-writer |
| 1.19    | 2026-09-04 | Documented the Phase 2 **agent run-history screen** (issue #121 / S-108, PR #140): a read-only, presentational + pure UI story with **no schema, data-model, or API change and no migration**. §2 front-end row and §9 `panel/` row now record the screen at `/agents/[slug]` — `app/agents/[slug]/page.tsx` (thin async server component declaring `force-dynamic`/`revalidate=0`/`fetchCache` **inline** per the S-104 audit D4 / §12 convention, reading all runs newest-first from `v_runs`, computing the params count off `params_schema`, and **404-ing on an unknown OR a disabled slug** — a disabled agent is not-found, never an empty list), `lib/domain/run-row.ts` (the pure, **clock-injected** row shaper + agent-header metric derivations — p50 via lower-median so the reported time is a real observed run, success rate, run count — deriving **every** emitted status, rows AND the success-rate metric, through the shared `lib/domain/status.ts` `effectiveStatus` per FR11a so a stale `running` run presents `timed_out` and the header can never disagree with the rows, never restating thresholds; plus `shouldRunHistory404` and the §8.2 `outcomeLabel`), two new `lib/supabase/queries.ts` helpers (`getAllRunsByAgentSlug` — unbounded newest-first history paged with `.range()` below PostgREST `max_rows` so counts/metrics stay true beyond 1,000; `getStepProgressForRuns` — a single grouped read, **never N+1**, folding `done=non-pending/total` for the `n/m` Steps column), and `components/runs/{AgentHeader,RunHistoryTable,RunHistoryRow}.tsx` (semantic `<table>` markup per spec §10, token-only CSS Modules on the DESIGN §4.4 grid, §5.2 fields). Added `docs/runbooks/issue-121-ac10-reaper-paused.md` (the AC10 reaper-paused verification procedure + evidence). §11 adds the S-108 Layer 1 (`run-row`, 28) + Layer 2 (`run-history` 17, `run-history-edge` 7) + Layer 2.5 (`runs-by-agent`, 5, ran live) test surface with `coverage_gate` PASS and `make validate` green both branches (Python 436 + panel 510 passed / 4 skipped). Recorded deferrals (not missing scope): filters/repo-chips/search/pagination + the "live" indicator (PRD §10), the inline PR/error-detail repo cell (S-109/S-110), the `/runs/[id]` forward link + enabled Invoke (S-113 — Invoke renders disabled), and the 1024px pixel geometry (Playwright, S-114). Current-state status correction only — the SD2 server-only boundary, SD4 `effectiveStatus`, inline route-config, semantic-table spec §10, and Nocturne token discipline all trace to existing decisions/ADRs, so no new architectural decision and no new ADR is required (precedent: rows 1.11/1.12/1.13/1.14/1.15/1.16/1.17/1.18). No schema/data/API change; no `AGENTS.md`, user-guide, or OpenAPI parity triggered (none exist yet — the panel is a pre-deploy scaffold with no API surface). The verifier's Minor/Intended drift — D4, the DESIGN §8.2 outcome-tag set omits the `N/A` tag the impl renders for `not_applicable` — is routed to `product-engineer` for DESIGN write-back, not made here. | technical-writer |
| 1.20    | 2026-09-06 | Settled **OQ2** — the panel→agent `prompt`-wrapping question — by live observation against the deployed AgentCore runtime, closing the last Wave 4 items recorded BLOCKED (plan tasks 1.17, 1.27 and the OQ2 exit criterion). Added a §8 subsection **"Panel invocation payload — bare JSON, no `prompt` wrapper"** recording that the panel sends the bare inner JSON (`panel/lib/aws/invoke.ts`) and must not add a wrapper: a bare-sent payload reached `running` and entered the pipeline (run `a7203345-…`, `running` 20:12:31 UTC, `GitHub credentials resolved` 20:12:32), while a deliberately malformed bare payload was rejected `INVALID_PARAMS` under the `missing_fields` classification (session `209f483b-…`) — never the `wrapper_only` "appears double-wrapped" branch, which a 12-hour log sweep shows has never fired. The subsection also records the honest evidentiary limit: the agent never logs the raw payload and `unwrap_payload` is *total* (bare/single/double all resolve identically), so the literal pre-unwrap bytes are neither logged nor externally inferable — what is established is that the panel's bare payload is accepted end to end, and that the repeated-unwrap tolerance stays relevant to the `agentcore` **CLI** path (#97), not the panel's SDK path. Verification procedure + evidence: [`runbooks/issue-89-live-verification.md`](runbooks/issue-89-live-verification.md). Current-state status correction only — it answers a pre-existing open question with a measurement and changes no enforceable rule, no schema/data/API, and no code, so no new ADR is required (precedent: rows 1.3/1.4/1.6/1.7/1.11–1.19). | developer |
| 1.21    | 2026-09-07 | Documented the Phase 2 **run-detail screen** (issue #122 / S-109, PR #150): a read-only, presentational + pure UI story with **no schema, data-model, or API change and a documented migration opt-out**. §2 front-end row and §9 `panel/` row now record the screen at `/runs/[id]` — `app/runs/[id]/page.tsx` (+ `page.module.css`), a thin async **server component** declaring `force-dynamic`/`revalidate=0`/`fetchCache="force-no-store"` **inline** (S-104 audit D4 / §12; a run's derived status and log tail change second-to-second, exactly the staleness FR11a prevents), reading run/steps/events/artifacts via the existing S-104 `getRunById`/`getRunSteps`/`getRunEvents`/`getRunArtifacts`, `notFound()` (404) on an unknown id, and a `"use server"` `loadEarlier` server action; three new pure `lib/domain/` modules — `artifact-url.ts` (`isSafeArtifactUrl`, the mandatory **security guard #5**, spec §12/A10 — a total, never-throwing WHATWG-`URL` https-only check so an agent-written `run_artifacts.url` that is `javascript:`/`data:`/`http:`/relative renders inert), `log-window.ts` (the SD11 most-recent-2,000 `selectRecentWindow` — `seq`-sorted because agent buffering means arrival order ≠ emission order, D5 — plus the gap-free `priorWindowRange` load-earlier math), and `run-detail.ts` (`selectBanner` DESIGN §8.3 terminal-state selection, `buildLogLines`, `buildSummary` — status derived through the shared `lib/domain/status.ts` `effectiveStatus`, never the raw column); `components/run-detail/{RunSummary,ArtifactLinks,LogViewer,StateBanner}.tsx` (token-only CSS Modules — `ArtifactLinks` renders on failed runs too, AC14, https-only + `rel="noopener noreferrer"`; `LogViewer` is the client 4-column `LogLine` grid ordered by `seq`, message rendered **inert — never `dangerouslySetInnerHTML`**, security guard #6, `aria-live="polite"`, "load earlier", and a stable `data-sse-mount="run-log"` hook left for S-110; `StateBanner` per DESIGN §8.3 carrying the reaper's `error_message`); and `getRunEventsInRange` added to `lib/supabase/queries.ts` (the load-earlier windowed read, paged under PostgREST `max_rows`). DESIGN §4.2 (full-height, log region owns the scroll), §5.3, §7.5 (log never truncated), §8.3 conformance reviewed. §11 records the S-109 test surface as already added by `qa-engineer` (not duplicated here); `make validate` exits 0 (Python 452 + panel 711 passed / 4 skipped; Layer 2.5 ran live), `coverage_gate` PASS, verifier audit High fidelity 9/9 Pass (highest drift Minor). Current-state status correction only — the SD2 server-only boundary, SD4 `effectiveStatus`, inline route-config, SD11 log window, the §12/A10 security guards, and Nocturne token discipline all trace to existing spec/PRD decisions and prior ADRs, so no new architectural decision and **no new ADR is required** (precedent: rows 1.11–1.19). No schema/data/API change (migration opt-out documented); no `AGENTS.md`, user-guide, or OpenAPI parity triggered — none exist yet (the panel is a pre-deploy scaffold with no API surface). §12 and §16 are unaffected (no new enforceable rule, no new dependency). The verifier's Minor drift findings are routed to `product-engineer`, not written back here. | technical-writer |
| 1.22    | 2026-09-08 | Documented the Phase 2 **SSE live-tail relay** (issue #123 / S-110, PR #152): a read-only UI/relay story with **no schema, data-model, or API-table change** (migration is a documented **N/A opt-out**). §2 front-end row and §9 `panel/` row now record the panel's **first `app/api/**` route handler** — `app/api/runs/[id]/events/stream/route.ts`, `GET /api/runs/[id]/events/stream`, `text/event-stream`, an `after_seq` cursor coerced to a non-negative integer (else 0), route-segment config (`force-dynamic`/`revalidate=0`/`fetchCache`/`runtime="nodejs"`) declared **inline** (S-104 audit D4 / §12; a stream must never be cached/statically rendered), that **backfills then subscribes** via a **server-side** Supabase Realtime subscription (SD2 — the browser holds no Supabase credentials), heartbeats every 15 s, closes with a `{reason}` frame on terminal/abort, unsubscribes exactly once, and writes the SR5 open/close structured-log pair keyed by `run_id` + last `seq` (§13) — and the pure DI layer `lib/sse/{cursor,serialize,autoscroll,relay}.ts` (`relay.ts` owns the SD6 backfill→subscribe→drop-at-or-below sequencing + lifecycle logging; `cursor.ts` the gap-free `SeqCursor`/`dedupeAndOrder` ordering strictly by the monotonic agent-assigned `seq` because buffering means arrival order ≠ emission order, D5; `serialize.ts` the newline-safe frame serializer/parser over `event`/`run`/`heartbeat`/`closed`, JSON-encoded so a payload can never forge the blank-line terminator; `autoscroll.ts` the DESIGN §6.6 inclusive-24px predicate), `lib/hooks/useRunStream.ts` (the `EventSource` hook — client-side `seq` dedupe, stop-after-`closed`, reconnect on an *unexpected* drop with the highest rendered `seq` as `after_seq` so no line is lost), `components/run-detail/{LiveLogViewer,LiveTailButton}.tsx` (the AC5 live status re-derived through the shared `lib/domain/status.ts` `effectiveStatus` so a raw Realtime `running` push on an expired run still reads `timed_out` (SD4); the AC6/§6.6 autoscroll/pause/resume; message rendered **inert — never `dangerouslySetInnerHTML`**, security guard #6), the `app/runs/[id]/page.tsx` selector (`LiveLogViewer` when the **effective** status is non-terminal vs the S-109 `LogViewer` for terminal runs), and the new `getRunEventsAfterSeq` query helper (relay backfill — `seq > afterSeq`, ascending, paged below PostgREST `max_rows`, **unbounded upper end**: an earlier `Number.MAX_SAFE_INTEGER` sentinel ceiling overflowed the `integer` `seq` column, pg 22003, caught by the live Layer 2.5 run). DESIGN §6.6 conformance reviewed. §11 records the S-110 test surface as already added by `qa-engineer` (not duplicated here); all 5 quality gates pass, `make validate` green both branches (Python 452 + panel 769 passed / 4 Docker-gated skips), Layer 2.5 `stream-e2e` **ran live** (3/3 deterministic), `coverage_gate` **PASS**, verifier audit High fidelity 8/8 Pass (highest drift Minor, all Intended). Current-state status correction only — the relay-not-browser-subscription boundary traces to the pre-existing **SD2/SD6/F2** decisions, the live-status derivation to **SD4** (`effectiveStatus`), inline route-config to the S-104 D4 convention (§12), and Nocturne token discipline to S-101/S-105, so no new architectural decision and **no new ADR is required** (precedent: rows 1.11–1.21, all current-state updates without a new ADR). No schema/data/API-table change (migration opt-out documented). Drift/stale-doc check: **no `AGENTS.md`, user-guide, or OpenAPI parity obligation is triggered — none exists yet** (the panel is a pre-deploy scaffold with no API-doc surface). Flagged for `product-engineer`: this story adds the panel's **first HTTP endpoint**, so an API-surface doc (`docs/api/openapi.yaml` + `docs/api/endpoints.md`) may now be warranted as the panel's HTTP surface grows — routed, not created here (documenting one internal `text/event-stream` relay endpoint before a deploy target or a second endpoint exists would be premature). The verifier's Minor/Intended drift findings are routed to `product-engineer`, not written back here. | technical-writer |
| 1.23    | 2026-09-08 | Documented the Phase 2 **Playwright E2E suite + CI Layer 2.5 gating** (issues #127 + #134 / S-114, PR #153): a tests + CI story with **no product code and no schema/data/API change** (migration is a documented **N/A opt-out**). §11 gains an **E2E suite + CI Layer 2.5 gating** bullet recording the live Playwright suite at `panel/tests/e2e/` (config with retries 0 / one worker / headless-in-CI / `webServer` = `next dev`, `global-setup.ts` = stack reset+verify + migrations/seed + seeded-catalog assertion + local-only `service_role` grant per §7 + Realtime warm-up + AgentCore HTTP-boundary stub via `AWS_ENDPOINT_URL_BEDROCK_AGENTCORE`, fixtures, and 7 scenarios + edge cases across `invoke`/`live-tail`/`stale-and-artifact`/`density`/`edge-cases`.spec.ts with the full scenario→AC map 1→AC12 / 2→AC6 / 3→SD6 / 4→AC10 / 5→AC13 / 6→AC9 / 7→AC14, all 10 E2E tests passing live incl. a cold start), the `panel/tests/integration/e2e-fixture.smoke.test.ts` Layer 2.5 seed-fixture smoke, and the CI change (`.github/workflows/ci.yml` panel job boots the Supabase stack, sets `REQUIRE_LOCAL_DB=1`, and runs the gated Layer 2.5 `test:coverage` + `test:e2e`); the §11 front-end (`panel`) row is flipped from "Playwright config stub … no committed scenarios yet" to the shipped suite, and the **standing G2 obligation** note in the S-104 row is flipped to **RESOLVED** — `panel/tests/integration/db.ts` `probeLocalDb` makes a Docker-gated Layer 2.5 skip a hard failure under `REQUIRE_LOCAL_DB=1` uniformly across all thirteen suites, so a Layer 2.5 skip can no longer pass CI vacuously (both gate directions demonstrated RED then reverted — `workstream/s114-negative-demos.md`). `coverage_gate` **PASS**; verifier audit **High fidelity 9/9 ACs** (highest drift Minor, all Intended); `make validate` exits 0 (Python 452 + panel 774 passed / 4 `RUN_BUNDLE_SECRET_TEST`-gated skips; E2E 10 passed). `TESTING.md` (CI-vs-local skip policy + scenario-to-AC table + E2E/Layer-2.5 package rows) was already updated by the developer and is not re-authored here. Current-state status correction only — the E2E surface and CI reachability trace to the pre-existing spec §14 test strategy and the standing G2 decision (a Layer 2.5 skip must not pass CI), so no new architectural decision and **no new ADR is required** (precedent: rows 1.3/1.4/1.6/1.7/1.11–1.22, all current-state updates without a new ADR). Drift/stale-doc check: **no `AGENTS.md`, user-guide, or OpenAPI parity obligation is triggered — none exists yet** (the panel is a pre-deploy scaffold with no user-guide/API-doc surface). Two current-state findings are flagged to `product-engineer`, **not** fixed here: (1) `next build` fails on the S-110 SSE route's non-route `parseAfterSeq` export — a pre-existing latent defect that does not affect the E2E run (it uses `next dev`); (2) the §7 local `service_role` grant asymmetry is reproduced in `global-setup.ts` (service_role only, RLS deny-all preserved). | technical-writer |
| 1.24    | 2026-09-08 | Documented the Phase 2 **panel deploy configuration + privacy release gate** (issue #128 / S-115, PR #154): the **committable half only** — the panel remains a **pre-deploy scaffold; it is NOT deployed** (config + gate committed, the live deploy is operator-gated per the runbook). §13 now records the shipped `panel/Dockerfile` + `panel/.dockerignore` (Next.js `standalone` production image built from the monorepo-root context, no secrets baked in, verified to build + boot), `panel/next.config.ts` `output: "standalone"`, `panel/fly.toml` (**private-only by construction** — no `[http_service]`/public ports, SR2/D16 banner, app `dt-agent-fleet-panel`/region `iad`), the fail-closed privacy release gate (`scripts/verify-fly-private.sh` + the unit-tested pure parser `panel/scripts/fly-privacy-check.mjs`), the `next build` unblock (the S-110 SSE route's non-route `parseAfterSeq` export moved into `panel/lib/sse/cursor.ts`, behavior unchanged — resolving the latent defect flagged at S-114), and the CI privacy-gate parser test + `shellcheck` steps; it states plainly that the **live half is a documented operator procedure, not yet executed** ([`runbooks/panel-deployment.md`](runbooks/panel-deployment.md)): `fly deploy`, AWS OIDC IdP + IAM role/trust-policy, `fly secrets`, the live OIDC socket probe, the `DurationSeconds: 900 ≤ MaxSessionDuration` check, and the live end-to-end invocation. §5 reframes the "no user auth in v1" note to state that keeping the Fly app private is the panel's **only** security boundary (SR2), and updates the "pending Fly-endpoint verification" bullet — the OIDC socket-shape probe / `sub` normalization / `MaxSessionDuration` check are now an exact operator procedure in the runbook (Impl Step 5), runnable only against a live Machine, so **spec OQ1 stays OPEN** (this PR did not run the live probe). §6 adds the enforceable **"panel privacy is a release gate, not a preference (SR2 / D16)"** rule (private-only `fly.toml` + fail-closed `verify-fly-private.sh` after every deploy). §11 is left to `qa-engineer`'s `TESTING.md` (S-115 rows already present — privacy-gate parser 21 tests / CLI exit-code smoke / `parseAfterSeq` re-point); referenced, not duplicated. Current-state status correction only — the privacy-release-gate mechanism traces to the pre-existing **SR2/D16** spec decisions and the `parseAfterSeq` move is a behavior-preserving defect fix, so no new architectural decision and **no new ADR is required** (precedent: rows 1.11–1.23, all current-state updates without a new ADR). **No §16 change** — `output: "standalone"` is config only and the CI `shellcheck` step uses the runner-provided binary, not an npm dependency; no runtime dependency changed. No schema/data/API-table change (migration is a documented **N/A opt-out**); no `AGENTS.md`, user-guide, or OpenAPI parity triggered — none exists yet (the panel is a pre-deploy scaffold). Drift fixed in the same cycle: `panel/README.md` (S-114 "lands in S-114" Stack/Scripts wording flipped to shipped) and the stale `panel/playwright.config.ts` comment claiming the `parseAfterSeq` defect is unfixed and "flagged to product-engineer as drift" (S-115 fixed it). Routed to `product-engineer` (**not** written back to the spec/PRD here — that is `product-engineer`'s territory and those files were not touched): (1) the **OQ2** spec §17 / PRD §18 write-back can be done now (resolved via #89, 2026-09-06); (2) the **OQ1** spec §17 / PRD open-question-#5 write-back must wait until the operator runs the live probe (runbook Impl Step 5) — OQ1 is not resolvable from this PR. | technical-writer |

| 1.25    | 2026-09-09 | Documented the Phase 2 panel **`(panel)` route-group restructure** (issue #157 / S-118, PR #164): a **pure mechanical** `git mv` of the authenticated routes (`app/page.tsx`, `app/agents/**`, `app/runs/**`, `app/dev/**`) into `app/(panel)/` — R100 byte-identical, **no behavior, schema, data-model, or API change** (migration is a documented **N/A opt-out**). A new `app/(panel)/layout.tsx` now renders `AppShell`, and the root `app/layout.tsx` **no longer wraps `AppShell`** (it keeps only `<html>`/`<body>`, the Inter fonts, metadata, and the global CSS imports) so a future public route (`/login`, S-119) can render outside the shell; the `(panel)` parentheses do not appear in URLs, so `/`, `/agents/[slug]`, `/runs/[id]` resolve exactly as before and `app/api/**` is unchanged. §9 `panel/` row: corrected the one now-inaccurate current-state sentence — `AppShell` wraps the authenticated routes via `app/(panel)/layout.tsx` (was "wraps `app/page.tsx` via `app/layout.tsx`") — and appended a compact route-group note stating the move + the root-layout de-wrapping, so the row's earlier `app/<page>` references denote the same files now under `app/(panel)/` without rewriting each historical clause. §2 front-end row: added a one-line S-118 marker pointing the file-layout summary at the route group. Separately corrected the stale `panel/README.md` app-shell bullet ("`app/layout.tsx` wraps `children` in it" → the `(panel)` route-group layout wraps it; root layout keeps only the frame). Only 3 page-wiring test import paths changed on-branch (not re-authored here). Current-state status correction only — the route-group idiom traces to the existing S-118 story and standard Next.js App Router conventions, no enforceable rule / architectural constraint / quality gate / security rule / observability baseline changed, so **no new ADR is required** (precedent: rows 1.3/1.4/1.6/1.7/1.11–1.24, all current-state updates without a new ADR). No schema/data/API change; no `AGENTS.md`, user-guide, or OpenAPI parity triggered — none exists yet (the panel is a pre-deploy scaffold; the one internal SSE endpoint remains the only HTTP surface, unchanged). | technical-writer |
| 1.26    | 2026-09-09 | Documented the Phase 2 panel **login screen + sign-in action** (issue #158 / S-119, PR #166): the panel's only **public** screen, at `/login`, rendered outside the `(panel)` route group (no `AppShell`). **No schema/data-model/API-table change** — auth state lives in Supabase `auth.*` + session cookies (migration is a documented **N/A opt-out**). §5 (**Authentication & Authorization**) was materially stale and is corrected: its opening no longer asserts "No user authentication in v1 / the panel runs without login" (true in Phase 1, now false after the S-116→S-119 auth wave). §5 now records the current state — Supabase Auth password sign-in only (no signup/reset/confirmation/magic-link, invitation-only), the separate anon-key auth client family (SA1) that never touches the service-role data client with **RLS still deny-all (D11)**, the S-117 middleware chokepoint (`getClaims()` not `getSession()`, fail-closed, 302-`/login` for `ui` vs 401-JSON for `api`/SSE), and the `/login` screen with its three load-bearing security properties (anti-enumeration — unknown-email ≡ wrong-password, one generic message; redirect safety — `safeRedirectTarget` server-side before use; password never stored/logged/echoed) — noting sign-out UI is S-120. **The historical D16/R1 decision-record text is NOT rewritten here** — the D16 reversal / "no-auth" decision-record removal in the spec/PRD (and the §18 R1 row) is **S-122 scope** (recorded S-117 decision); §5 flags this. The SR2 private-app rule stays the operative deploy boundary until auth is deployed (the panel is still a pre-deploy scaffold), so §6/§13 are unchanged. §9 `panel/` row: appended the S-119 current-state (`app/login/*`, `components/auth/{LoginForm,PasswordField}.tsx`, reuse of S-116 `lib/auth/*` + `lib/supabase/auth-server` and the S-117 gate — not reimplemented). §11: added the S-119 test surface (unit `login-action` 16, component `LoginForm` 8, Layer 2.5 `auth-login` 4 ran live incl. RLS-deny-all-unchanged §14.3, E2E `auth.spec` + `auth.setup` project with `storageState`); `coverage_gate` PASS; `make validate` green (Python 452 + panel 915 / 4 gated skips), `next build` green with `/login` dynamic, E2E 17 passed. **DESIGN.md §5.5 + changelog v1.2** (added by the developer) were reviewed and are consistent + complete — the generic message "Invalid email or password." matches `INVALID_CREDENTIALS_MESSAGE`, the masked-default `SHOW`/`HIDE` toggle, the dead `aria-disabled` "Forgot password?" span, and the token-only CSS all match the shipped code; no change made. Current-state status correction only — the login screen implements existing **SD2/SA1**, anti-enumeration (§8.2), and redirect-safety (§8.1) decisions and the S-116/S-117 auth mechanisms, so no enforceable guideline changed and **no new ADR is required** (precedent: rows 1.11–1.25, all current-state updates without a new ADR). No `AGENTS.md`, user-guide, or OpenAPI parity triggered — none exists yet (the panel is a pre-deploy scaffold; the one internal SSE endpoint remains the only HTTP surface, unchanged — `/login` is a page route, not an API). Routed to `product-engineer` (**not** written back here): the D16-reversal / no-auth decision-record write-back to the spec §17 / PRD / this §5's historical framing, which is **S-122 scope**; and the verifier's one non-blocking coverage gap (D2 — the already-authenticated-visit redirect lacks a dedicated automated test) to `qa-engineer`. | technical-writer |

| 1.27    | 2026-09-09 | Documented the Phase 2 panel **logout route + sidebar Log out affordance** (issue #159 / S-120, PR #167): the sign-out half of the auth wave. **No schema/data-model/API-table change** — auth state lives in Supabase `auth.*` + session cookies (migration is a documented **N/A opt-out**). §5 (**Authentication & Authorization**): replaced the "There is no logout UI yet — sign-out lands in S-120" line with the shipped current state — a new **"Sign-out (`POST /api/auth/logout` + the sidebar affordance, S-120)"** bullet recording the **POST-only** route handler (the panel's **third `app/api/**` endpoint** — after the S-110 SSE stream and the S-112 `POST /api/agents/[slug]/invoke` route; the S-120 code comments say "second", overlooking the S-112 invoke endpoint, so the accurate count of three is recorded here — no `GET` export so a `GET` is a 405, inline route-segment config since a logout response carries cleared `Set-Cookie`, best-effort/idempotent `signOut`, always `302 → /login`, **never 500s**), the two load-bearing properties (logout is **reachable with an expiring/invalid session** because `lib/auth/route-policy.ts` classifies the **exact** path `/api/auth/logout` as `public` so the S-117 gate short-circuits before auth — exact match + POST-only, so no read surface widened, siblings stay `api`; and **the affordance performs no auth I/O — SD2/SA1 preserved** — the `(panel)` layout resolves `authenticated` server-side via `getClaims()` fail-closed and threads a static per-request prop into `AppShell → Sidebar`, the shell never calls Supabase so the S-106 hydration contract is untouched, the anon-key auth client family is used never the service-role data client, **RLS stays deny-all (D11)**). §9 `panel/` row: appended the S-120 current state (`app/api/auth/logout/route.ts`, `components/shell/LogOutItem.tsx`, the `authenticated`-prop threading through `Sidebar`/`AppShell` + `app/(panel)/layout.tsx`, `LogOutIcon → Phosphor Power`, and the `route-policy.ts` reclassification). §11: added the S-120 test surface (unit `logout-route` + `auth-route-policy`, component `LogOutItem` + `panel-layout-wiring`, E2E `auth.spec` logout scenarios); `coverage_gate` PASS; verifier audit High fidelity 9/9 ACs; `make validate` exits 0 (Python 452 + panel **898 passed / 49 skipped**; lint/format/typecheck/audit green), `next build` green with `/api/auth/logout` dynamic, E2E logout scenarios pass. **DESIGN.md §4.1 sidebar-footer Log out subsection + changelog v1.3** (added by the developer) were reviewed and are consistent + complete (footer order Log out below "System health" above "Collapse", POST-only form, collapsed accessible-name preserved, authenticated-only visibility, SD2 preserved) — the one small completeness gap fixed here: §4.1 and the v1.3 changelog cross-reference the Phosphor `Power` icon "in §10", but the §10 icon table had no such row, so a `⏻ Log out → Power` row was added to §10 (the icon is already wired in `components/icons.tsx`). Current-state status correction only — the logout path implements existing **SD2/SA1**, the S-116 route-policy/auth-server, and the S-117 gate; the POST-only + public-exact-path + inline-route-config choices all trace to existing security rules (§6) and the S-104 D4 convention (§12), so no enforceable guideline changed and **no new ADR is required** (precedent: rows 1.11–1.26, all current-state updates without a new ADR). The historical D16/R1 "no-auth" decision-record reversal remains **S-122 scope** — not rewritten here. **Drift/stale-doc check:** S-120 adds the panel's **third `app/api/**` endpoint** (after the S-110 SSE stream and the S-112 invoke route — the S-120 code comments' "second" is inaccurate, corrected in §5/§9); re-evaluated the S-110 note that an API-surface doc (`docs/api/openapi.yaml` + `docs/api/endpoints.md`) *may* become warranted — still **premature and deliberately not created**: both endpoints are internal panel-only mechanisms (an SSE relay and a form-POST logout that 302-redirects), there is no external/consumer API contract, no versioned surface, and no deploy target yet (pre-deploy scaffold). The API-doc trigger is **re-routed to `product-engineer`** to reconsider once a consumer-facing/external endpoint or a deploy target lands (the three current endpoints are all internal panel-only mechanisms). No `AGENTS.md` or user-guide parity triggered — neither exists yet. | technical-writer |

| 1.28    | 2026-09-10 | Documented the Phase 2 panel **live-tail 401 handling** (issue #160 / S-121, branch `story/S-121-live-tail-401`): the auth-consequence fix on the **client** side of the S-110 SSE relay. **Panel-only, presentational + hook logic — no schema/data-model/API-table change** (migration is a documented **N/A opt-out**); `lib/sse/relay.ts` sequencing is untouched, no new dependency, no new component library. §9 `panel/` row: rewrote the `useRunStream` current-state to record that the hook now tracks whether the *current* attempt ever opened (`onopen`, or the first `event`/`run` frame) and treats an `onerror` on a **never-opened** attempt with `readyState === CLOSED` as a **terminal** authorization stop — no reconnect, no timer — exposed as `authStopped: true`, while a previously-opened drop keeps the S-110 reconnect with the highest rendered `seq` and `SeqCursor` dedupe intact; and that `LiveLogViewer` renders a token-only `role="alert"` session-expired notice (`LogViewer.module.css` `.sessionExpired`) and drops the live-tail control on `authStopped`. §2 front-end row: added a compact S-121 marker. §5 (**Authentication & Authorization**): extended the SSE-gate bullet to note that the `401` is returned **before** the `text/event-stream` response opens (an `EventSource` cannot send headers), so the never-opened + `CLOSED` signal — not a `closed{reason:"unauthorized"}` frame — is the client's authoritative unauthorized tell. §11: added the S-121 test surface (unit `use-run-stream-auth` 6, +2 in component `live-log-viewer`). **This settles spec OQ3** (the SSE gate returns a plain 401 before the stream opens, and the client stops reconnecting) — recorded here as a current-state fact; the OQ3 spec-§17 / PRD write-back is **routed to `product-engineer`** (spec/PRD are not touched here). Current-state status correction only — the fix is a client-side consequence of the existing **SD2** server-only relay (S-110), the **S-117** middleware gate's `401` for `api`/SSE routes, and the S-110 reconnect/`SeqCursor` contract; it introduces **no new enforceable rule, decision, quality gate, security rule, or observability baseline**, so **no new ADR is required** (precedent: rows 1.11–1.27, all current-state updates without a new ADR). Verification (reported by `qa-engineer`, consistent with the audited code): `make validate` green (Python **452 passed** + panel **906 passed / 49 gated skips**), `stream-e2e` ran live and passed, `next build` green, `coverage_gate` **PASS**, verifier audit **High fidelity 6/6**. **Drift/stale-doc check across the live-tail / SSE-gate surface:** `clean` after this pass — DESIGN.md has no `useRunStream`/reconnect-behavior text to update (§6.6 covers only autoscroll, still accurate); the §5 gate bullet, the §2 marker, and the §9 hook description are now mutually consistent (all three name the pre-open-401 terminal stop). No `AGENTS.md`, user-guide, or OpenAPI parity triggered — none exists yet (the panel is a pre-deploy scaffold; the two internal `app/api/**` endpoints — the SSE relay and the S-120 logout, plus the S-112 invoke route — remain internal panel-only mechanisms with no external/consumer contract, so the API-doc trigger stays deliberately deferred and re-routed to `product-engineer`). | technical-writer |

| 1.29    | 2026-09-10 | Documented the Phase 2 panel **auth release gate replacing the privacy gate** (issue #161 / S-122, PR #169). **This is a genuine enforceable-rule change** — the security boundary flips from network-privacy to login — so it is recorded with a new **[ADR-007](adr/ADR-007-auth-release-gate-replaces-privacy-gate.md)** (unlike the current-state-only rows 1.11–1.28). No schema/data-model/API-table change (auth state lives in Supabase `auth.*` + cookies; migration is a documented **N/A opt-out**). The S-115 privacy release gate (`scripts/verify-fly-private.sh` + `panel/scripts/fly-privacy-check.mjs`) was the mechanized form of "privacy is the only boundary" (SR2/D16); the auth wave reverses D16, so the gate is **replaced, not deleted** — a release must never run with no boundary check. Shipped: the pure fail-closed parser `panel/scripts/panel-auth-check.mjs` (env-name presence, protected UI → 302 /login, SSE → 401, signUp rejected — no I/O, unit-testable) + the live wrapper `scripts/verify-panel-auth.sh` (HTTP probes against a **hostname arg** so the same gate works over the private network in Phase A and publicly in Phase B; `fly secrets list` NAMES only; a disposable-address `signUp` attempt that deletes any account it creates; never logs secret values), with the old privacy pair removed and its unit tests re-pointed to the new parser (`panel/tests/unit/panel-auth-check.test.ts`, 30 tests). **§6** enforceable rule rewritten: "panel privacy is a release gate" → **"the auth boundary is a release gate, asserted by observation"** (the signup-disabled check is the highest-value assertion — PRD AC17 / R9). **§5** notes the boundary is now login-enforced and mechanically gated (the SR2 private-app rule no longer the operative boundary; still private in this story). **§13** deploy section re-pointed from the privacy gate to the auth gate + the two-phase rollout (Phase A private / Phase B go-public S-123). **§18** R1 flipped to **Resolved** (auth exists + mechanically gated) and **R8** (public-login brute force, accepted with Supabase rate limits) added. `.github/workflows/ci.yml` runs the parser unit tests by name + `shellcheck`s the new wrapper (replacing the S-115 privacy steps). `panel/fly.toml` banner rewritten to state the auth boundary while **keeping the app PRIVATE** (no `[http_service]`, no public IP — going public is S-123). RED-then-reverted both-directions evidence in `workstream/s122-negative-demos.md`; the operator runbook `docs/runbooks/panel-deployment.md` now carries the Supabase config checklist, the Phase A private-verification procedure, and the full Phase B go-public procedure + one-command `fly ips release` containment. Quality gates: `pnpm run test:unit`/`test`/`validate`/`build` green, `shellcheck` clean, `make validate` green; `coverage_gate` PASS on the new pure parser. The D16-reversal / R1 write-back to the **spec §17 / PRD** decision records remains **`product-engineer` territory** (routed, not made here); this row + ADR-007 record the enforceable-rule change in the guidelines. | developer |

## 1. Overview

The system has three pieces with different languages and runtimes, joined by Supabase as the *system of record*:

1. **Front-end / panel** — Next.js in TypeScript, deployed on Fly.io (Phase 2).
2. **Agents** — containers in AWS Bedrock AgentCore, in Python, that report their own lifecycle (Phase 1).
3. **Database** — PostgreSQL via Supabase, with Realtime for live log tailing and `pg_cron` for stale-execution detection (Phase 1).

Guiding principle: **the agent reports state explicitly**; nothing is reconstructed by inference over logs (see Problem Statement in `product-context.md`). Every decision in this document upholds that rule or resolves a consequence of it (timeouts, buffering, credentials).

## 2. Technology Stack

| Layer | Technology | Notes |
|---|---|---|
| Front-end | Next.js (TypeScript) | Routes, JSON Schema-generated forms, live tail via Supabase Realtime (Phase 2). The `panel` package (Next.js 15 App Router, React 19, TS strict) is **scaffolded in the repo** as of issue #114 / S-101, with the Nocturne **design-token layer + the twelve §11.2 primitives** shipped as of issue #118 / S-105 (`styles/tokens.css`, `styles/globals.css`, `components/*`, `lib/format.ts`), the **app shell** (collapsible sidebar + top bar + scrolling content region, DESIGN §4.1) shipped as of issue #119 / S-106 (`components/shell/*`, `lib/ui/{shortcuts,sidebar-state}.ts`), the **agents dashboard** at `/` (DESIGN §5.1 three density variants, read-only) shipped as of issue #120 / S-107 (`app/page.tsx`, `components/dashboard/*`, `lib/domain/dashboard.ts`, `lib/ui/density-state.ts`), and the **agent run-history screen** at `/agents/[slug]` (DESIGN §5.2, semantic `<table>`, read-only) shipped as of issue #121 / S-108 (`app/agents/[slug]/page.tsx`, `components/runs/*`, `lib/domain/run-row.ts`), the **run-detail screen** at `/runs/[id]` (DESIGN §4.2/§5.3/§8.3, read-only) shipped as of issue #122 / S-109 (`app/runs/[id]/page.tsx`, `components/run-detail/*`, `lib/domain/{run-detail,log-window,artifact-url}.ts`), and the **SSE live-tail relay** — the panel's **first `app/api/**` route handler**, `GET /api/runs/[id]/events/stream` (`text/event-stream`), plus the client live-tail screen it feeds at `/runs/[id]` (DESIGN §6.6 autoscroll) — shipped as of issue #123 / S-110 (`app/api/runs/[id]/events/stream/route.ts`, `lib/sse/{cursor,serialize,autoscroll,relay}.ts`, `lib/hooks/useRunStream.ts`, `components/run-detail/{LiveLogViewer,LiveTailButton}.tsx`); as of issue #160 / S-121 the live-tail hook treats a pre-open `401` (never-opened + `CLOSED`) as a terminal auth stop — no reconnect loop — and `LiveLogViewer` surfaces a session-expired notice (settles spec OQ3); as of issue #157 / S-118 the authenticated routes (`page.tsx`, `agents/**`, `runs/**`, `dev/**`) live under the `app/(panel)/` route group whose `layout.tsx` renders `AppShell` (mechanical `git mv`, same URLs, `app/api/**` unchanged — see §9); remaining routes/forms land in later Phase 2 stories |
| Front-end hosting | Fly.io | Node process persists after responding — enables fire-and-forget invocation without a durable queue (D7). Panel **not yet deployed** — scaffold exists, Fly config lands in a later Phase 2 story |
| Agent runtime | AWS Bedrock AgentCore | Containers; AgentCore controls the lifecycle and enforces the real timeout |
| Agent SDK | Python, stdlib only (`urllib`, `logging`, `atexit`) | Single file [`agent_reporter.py`](reference/agent_reporter.py), copied per repo (D13), not a pip package |
| Database | PostgreSQL via Supabase | System of record for executions. RLS deny-all from the first migration (D11) |
| Realtime | Supabase Realtime on `run_events` and `runs` | Live log tail without polling |
| Cron | `pg_cron` inside Supabase | Reaper for stale executions — does not depend on the front-end being alive (D10). Job `reap-stale-runs`, `* * * * *`, **scheduled and verified as of issue #94** (ADR-004) |
| Front-end AWS auth | Fly OIDC + STS `AssumeRoleWithWebIdentity` | No static keys in `fly secrets` (D12) |
| GitHub App secrets | AWS Secrets Manager | Only the ARN lives in `github_installations`, never the key |

## 3. Architecture Patterns

**Supabase as system of record, CloudWatch as infrastructure telemetry.** CloudWatch remains outside the panel's scope — it is an AgentCore implementation detail, not a source of business truth. However, if the API layer (PostgREST) is unreachable during Phase 1, the agent SDK falls back to writing payloads to stderr, which lands in CloudWatch as a safety net.

**Separation of `status` / `outcome` (D3).** The lifecycle of an execution (`queued → running → succeeded|failed|canceled`, plus `timed_out` and `failed_to_start` injected by the reaper) is one column. The business result (`fixed`, `partial`, `no_vulnerabilities`, `needs_review`, `not_applicable`) is another. An execution that finishes successfully and finds no issues is not a failure.

**Structured log, not blob (D4).** `run_events` is a table of rows, not a text field. Enables filtering by level and live tail via Realtime.

**Snapshot over live reference.** Each `run` copies `agent_version`, `params`, `max_runtime_seconds`, `grace_seconds`, and `start_timeout_seconds` at dispatch time. If the configured timeout of an agent changes later, historical runs are not re-evaluated against the new value. There is no `agent_versions` table in v1 — the per-run snapshot covers auditability without the extra table.

**Repositories as a first-class entity (D6).** Not a string inside `params`: the GitHub App token is issued per `installation`, and modeling `repositories` separately enables queries like "all runs for repo X" and honors `is_enabled`/`archived_at`.

**Fire-and-forget from the route handler (D7).** The invocation to AgentCore happens directly from the Next.js handler on Fly, without a durable queue hop (SQS/Lambda). Justified because the Fly Node process does not die after responding. If this assumption stops holding, this pattern must be revisited.

**Two clocks for two distinct failures (D8, D9).** `timed_out` (agent hung, clock = `started_at`) and `failed_to_start` (invocation that never started, clock = `queued_at`) are separate states and runbooks. They do not collapse into a single "did not respond."

**Two application layers for the reaper (see specification §6).** `pg_cron` materializes the state change and writes the explanatory `run_event` every minute (D10). The `v_runs` view computes `effective_status` at read time, so the UI never shows a run "running" that has already expired even if the reaper is one minute behind. The reaper materializes eventual truth; the view tells immediate truth. **Both layers are live and empirically confirmed** as of issue #94 ([ADR-004](adr/ADR-004-schedule-pg-cron-reaper.md)): the job is registered `* * * * *`, and on a real hung run the view read `running` pre-threshold and then agreed with `timed_out` after materialization. Note that the explanatory event exists **only** in layer 1 — an unscheduled reaper is therefore worse than a late one, because the view keeps the UI looking correct while `runs.status` stays wrong and the "why" is never written.

## 4. API Design Standards

**A single invocation endpoint per agent:** `POST /api/agents/{slug}/invoke`. Full flow (see specification §8):

1. The front-end validates `params` against `agents.params_schema` (JSON Schema) before calling AgentCore.
2. The front-end, not the database or the agent, generates the `run_id` (uuid) and inserts the `runs` row in `queued` (D1) — so an invocation where the agent never starts still leaves a record of the failure.
3. The front-end obtains/refreshes AWS credentials (see §5) and invokes `InvokeAgentRuntime` in a fire-and-forget manner.
4. If the invocation throws, the server marks the run `failed_to_start` immediately — that detection is not delegated to the reaper.
5. The server responds `202` with `run_id`; the front-end navigates to the detail view without waiting for the agent to finish.

**The invocation form is not hand-coded per agent (D2).** It is rendered from `agents.params_schema`. Adding a new agent is a row in `agents`, not a front-end deploy (v1 acceptance criterion #5).

**The repository does not live inside `params_schema`.** It is a first-class field that the front-end renders separately when `agents.requires_repository = true`. The agent's schema covers only its own parameters.

## 5. Authentication & Authorization

**User authentication now exists (Phase 2 auth wave, S-116 → S-119).** The
original Phase-1 scope decision was *no* user authentication (D16 / Risk R1 —
"the panel runs without login"); that decision has been **reversed in
implementation** by the Phase 2 auth wave, and the panel now signs operators in
before granting access. The historical D16/R1 decision-record text (spec §17 /
PRD, this §5's prior wording, the §18 risk table) is being written back
separately — the D16 reversal and the removal of the "no-auth" decision records
are **S-122 scope**, deliberately not rewritten in the per-story current-state
passes (recorded S-117 decision). What is shipped as current state:

- **Supabase Auth, password sign-in only (S-116/S-119).** Operators authenticate
  with an email + password against Supabase Auth. There is **no** signup, password
  reset, email confirmation, or magic-link flow — accounts are provisioned
  out-of-band (invitation-only; created in the Supabase dashboard, never seeded by
  a migration). No schema/data-model change: auth state lives in Supabase's
  managed `auth.*` schema plus the session cookies, not in the application tables.
- **Separate anon-key auth client family (SA1).** Authentication uses a cookie-backed
  anon (publishable) key client (`lib/supabase/{auth-server,auth-middleware,browser}.ts`,
  `lib/supabase/auth-env.ts`), validated fail-fast with `AuthConfigError`. It is
  **distinct** from the service-role data client (SD2, §7) and never touches it; the
  service-role key gains no `NEXT_PUBLIC_` twin. **RLS stays deny-all (D11):
  authenticating a user grants no row access** — the panel still reads server-side
  with the service-role key, and authentication is a gate, not a grant.
- **Authorization chokepoint — the middleware gate (S-117).** `panel/middleware.ts`
  is the single place authorization is decided. It runs on every non-static request,
  short-circuits `public` routes (`/login`) before any auth work, verifies identity
  with `getClaims()` (**never** `getSession()`, whose user object is not re-validated
  against the Auth server), and is **fail-closed** (any auth error, or an unknown
  route class, denies). A denied `ui` route redirects `302` to
  `/login?redirect=<encoded original path>`; a denied `api`/SSE route returns
  `401 {"error":"UNAUTHORIZED"}` JSON. On success it returns the cookie-handler
  response so a refreshed token reaches the browser. For the SSE stream this
  `401` is returned **before** the `text/event-stream` response opens (an
  `EventSource` cannot send headers), so the panel's live-tail hook treats a
  never-opened, `CLOSED` connection as a **terminal** unauthorized stop — no
  reconnect loop — rather than expecting a `closed{reason:"unauthorized"}`
  frame (issue #160 / S-121, settling spec OQ3; see the `useRunStream` current
  state in §9).
- **The public login screen (`/login`, S-119).** The panel's **only** public screen,
  rendered **outside** the `(panel)` route group (S-118) so it does not inherit the
  authenticated `AppShell`. A server component pre-sanitizes `?redirect` and redirects
  an already-authenticated visitor (verified via `getClaims()`) to `/`; the
  `"use server"` `signIn` action validates the fields, sanitizes the redirect target
  **before use**, calls `signInWithPassword`, and redirects to the sanitized target on
  success. Three security properties are load-bearing and MUST be preserved:
  - **Anti-enumeration:** an unknown email and a wrong password are indistinguishable —
    both collapse through `classifySignInError` to a single `AUTH_INVALID_CREDENTIALS`
    code and the identical generic message ("Invalid email or password."). No branch
    inspects *which* credential was wrong.
  - **Redirect safety:** the untrusted `?redirect`/form value passes through
    `safeRedirectTarget` (S-116) before it is ever used, so it can never send the
    operator off-origin, protocol-relative, or into a `/login` loop; it is always a
    safe same-origin relative path.
  - **No password exposure:** the submitted password is forwarded to Supabase over
    HTTPS and is never stored, logged, or echoed — a failed attempt returns only the
    generic message and the (already-sanitized) redirect, and the form clears the
    password field client-side.
- **Sign-out (`POST /api/auth/logout` + the sidebar affordance, S-120).** The operator
  ends a session from the **sidebar footer "Log out"** control (rendered below "System
  health" and above "Collapse", only when the request is authenticated — see DESIGN §4.1).
  It is a plain `<form method="post" action="/api/auth/logout">` submit button — **POST
  only, no client JS** — because a `GET` logout is CSRF-triggerable and can be fired by a
  prefetcher or link scanner. The route handler (`app/api/auth/logout/route.ts`, the
  panel's **third** `app/api/**` endpoint — after the S-110 SSE stream and the S-112
  `POST /api/agents/[slug]/invoke` route; the S-120 code comments call it the "second",
  overlooking the S-112 invoke route, so the accurate current-state count is three) builds
  the S-116 cookie-backed anon auth client, calls `signOut` **best-effort** (idempotent — a
  no-session call is a no-op, any transient auth error is swallowed), and always redirects
  `302 → /login`; it **never 500s**, so ending a session can never be blocked by an auth
  error, and there is no `GET` export (a `GET` is a 405, not a logout). Two
  properties are load-bearing and MUST be preserved:
  - **Logout is reachable even with an expiring/invalid session.** The S-117 gate
    classifies the **exact** path `/api/auth/logout` as `public` (in `lib/auth/route-policy.ts`,
    alongside `/login`), so the session-ending POST reaches the handler instead of being
    401'd before it can clear cookies. The match is exact and the surface is POST-only, so
    no read surface is widened — `/api/auth/logout/extra` and any sibling `/api/auth/*`
    path stay gated `api`.
  - **The affordance performs no auth I/O (SD2/SA1 preserved).** Whether "Log out" renders
    is decided server-side: the authenticated `(panel)` route-group layout resolves
    `authenticated` via the S-116 auth-server client + `getClaims()` (verified identity,
    never the spoofable `getSession()`; fail-closed — any error hides the control) and
    threads it as a static per-request prop into `AppShell → Sidebar`. The shell itself
    never calls Supabase, so the S-106 hydration contract is untouched. The logout path
    uses the anon-key auth client family (SA1), never the service-role data client, and
    **RLS stays deny-all (D11)**.
- **Session lifetime.** Sessions are configured to expire after 12 hours of inactivity
  (surfaced to the operator as login-screen fine print).

**Panel privacy remains the current deploy state until the app is taken public
(SR2 / D16, §6, §13).** The auth wave is committed but the panel is still a pre-deploy
scaffold, so keeping the Fly app private remains the operative deploy state until the separate
go-public step (S-123). **The security *boundary* that protects the invoke surface is now login,
mechanically asserted (issue #161 / S-122, [ADR-007](adr/ADR-007-auth-release-gate-replaces-privacy-gate.md)):**
the S-115 privacy release gate is **replaced** by the auth release gate
(`scripts/verify-panel-auth.sh` + `panel/scripts/panel-auth-check.mjs`), which proves on the
deployed app that unauthenticated requests are denied (protected UI → 302 /login, SSE → 401) and
that public signups are rejected. A public service is introduced only in Phase B (S-123), after that
gate passes over the private network, behind an explicit operator-confirmation gate and reversible in
one command (`fly ips release`). (The Phase-1 "shared-secret header, or keep the app private"
mitigations are superseded by the shipped auth gate for the login boundary.)

**Front-end authentication against AWS — no static keys (D12).**

- **On Fly:** OIDC token issued by the Machine's local socket (`/.fly/api`, `aud=sts.amazonaws.com`) → `AssumeRoleWithWebIdentity` against an IAM Role with a trust policy scoped to the app's wildcard `sub` (`<org>:panel-agentes:*`) → 15-minute credentials, cached in memory with a 60-second refresh margin.
- **Locally:** `fromNodeProviderChain()` — SSO profile, `~/.aws/credentials`, or environment variables. The SDK handles its own refresh.
- Both branches live in a single module ([`panel/lib/aws/credentials.ts`](../panel/lib/aws/credentials.ts), adopted from the former `docs/reference/credentials.ts` stub as of issue #124 / S-111), detected by `FLY_APP_NAME` + socket existence. Invocation code receives the credentials provider and does not know which branch it is on; `credentialSource()` exposes the active branch for diagnostics. The role ARN is read from `AGENT_RUNTIME_ROLE_ARN` (fail-fast if unset — a named configuration error, not an opaque invoke-time auth failure).
- **OIDC-token extraction fails fast on an unrecognized shape (F5 fix, S-111).** `extractOidcToken` accepts only a string `value` or `token` field; any other shape throws `FlyOidcShapeError` naming the *keys* received (never their values). The former `parsed.aud` / raw-`data.trim()` fallbacks are removed — forwarding the audience (`sts.amazonaws.com`) to STS as a web-identity token produced a misleading auth error that read as an IAM trust-policy problem. Errors carry a distinct taxonomy (§13, R6): `CREDENTIALS_UNAVAILABLE` (500) for any credential/OIDC/STS failure vs `INVOCATION_FAILED` (502) for a downstream `InvokeAgentRuntime` failure; nothing here ever logs the token, the STS response, or the assumed-role credentials.
- The IAM permission is scoped to `bedrock-agentcore:InvokeAgentRuntime` on the runtimes ARN, never `*`.
- **Pending verification against the real Fly endpoint** (contract-defined, not yet live-probed — **OQ1 still open**): the exact JSON response shape from the OIDC socket, the literal claim name `sub` as normalized by AWS in the trust policy, and that `DurationSeconds: 900` is compatible with the role's `MaxSessionDuration`. The provider is written to fail loudly (never a silent fall-through to the local chain) if the live shape differs. As of issue #128 / S-115 this probe is no longer "deferred to a future story" — it is a documented **operator procedure** with the exact `fly ssh console` + `curl --unix-socket /.fly/api …` command and a `credentials.ts`-correct-only-if-reality-differs rule in [`runbooks/panel-deployment.md`](runbooks/panel-deployment.md) (Impl Step 5). It can only be run against a **live** Fly Machine (the socket does not exist locally), so OQ1 stays open until the operator executes the deploy + probe; the S-115 PR shipped only the committable half.

**Agent credentials against Supabase — service role key from Secrets Manager (D15, R2).** The agent authenticates to PostgREST with the Supabase service role key, which grants full database access (bypasses RLS). The key is **not** stored as a plaintext environment variable in the AgentCore runtime config — the agent fetches it from AWS Secrets Manager at startup, alongside the GitHub App private key. This keeps the credential out of the runtime configuration visible in the AgentCore console and applies Secrets Manager's access controls (IAM policy on the agent's execution role) and audit trail (CloudTrail). The scope risk (full DB access vs. scoped writes) remains accepted for a single-tenant system (R2). The exit path is unchanged: when the fleet grows or untrusted code enters, mint a scoped JWT per run instead.

**GitHub App secrets.** The private key never lives in the database — only its ARN in Secrets Manager (`github_installations.private_key_secret_arn`). The agent reads it at runtime to issue a short-lived installation token.

## 6. Security Requirements

- **RLS enabled and deny-all from the first migration (D11).** Without explicit policies, only `service_role` can read/write (it bypasses RLS). Enabling RLS after having data is a more costly and risky migration — hence the rule to do it from day one.
- **No long-lived AWS keys.** No IAM user with static keys in `fly secrets` (see §5).
- **The auth boundary is a release gate, asserted by observation (auth wave, S-116…S-122; [ADR-007](adr/ADR-007-auth-release-gate-replaces-privacy-gate.md)).** This rule **replaces** the former "panel privacy is a release gate, not a preference (SR2 / D16)". In v1 the panel had no user authentication (D16), so its only boundary was network privacy, mechanized by the S-115 privacy gate (`scripts/verify-fly-private.sh` + `panel/scripts/fly-privacy-check.mjs`) that failed the release on any public IP/service. The auth wave **reverses D16** — the panel now requires a Supabase password login (`middleware.ts` fail-closed gate, `/login`, POST logout, SSE `401`) — so the boundary that protects the invoke surface is **authentication**. The privacy gate is therefore **replaced, not deleted** (a release must never run with no boundary check): the new auth release gate `scripts/verify-panel-auth.sh` (decision logic in the unit-tested pure parser `panel/scripts/panel-auth-check.mjs`) probes the **deployed** app at a hostname argument and **fails the release (exit non-zero)** unless (1) the auth env-var **names** are present (names only, never values), (2) an unauthenticated protected UI request returns `302 → /login` (not 200), (3) an unauthenticated SSE request returns `401` (not 200), and (4) an attempted `signUp` is **rejected** — a successful signup fails the release (PRD AC17 / R9: an open signup on an internet-reachable panel lets anyone self-register into the invoke surface). It is **fail-closed** — anything unreadable/unparseable/unconfirmable fails. **Scope:** S-122 ships this gate and keeps `panel/fly.toml` **private** (no `[http_service]`, no public IP); actually going public is the separate, operator-executed, separately-merged Phase B (S-123), taken only after the gate passes on the still-private app (spec §15.1) — a public app never exists without a proven gate, and containment is one command (`fly ips release`).
- **Param validation at two points.** The front-end validates against `params_schema` before invoking; the agent validates its own payload at startup and fails fast with `error_code = INVALID_PARAMS` if it does not match (mitigation for R4 — drift between the schema in the database and what the agent expects).
- **Log messages truncated to 8 KB** (`run_events.message`) to bound row size and the risk of a malformed message degrading the table.
- **4xx errors from the reporting SDK are not retried.** A contract error (invalid payload) does not improve with waiting; only transient errors (5xx, network) use 3-retry backoff.
- **LLM fix agent — sandboxed tools + deterministic mandate backstop (ADR-001).** The `dependency-update` agent's optional LLM fix loop (`fix_agent.py`, reached only when validation fails after a dependency update in `llm_fix` mode) runs a Strands/Bedrock agent with **exactly five** tools (`shell`, `read_file`, `write_file`, `find_files`, `grep_code`). Every path-taking tool resolves against the workspace root via `_safe_path`, which rejects absolute paths, `../` traversal, and symlink escapes; `shell`/`find_files`/`grep_code` are confined to the workspace cwd. The system prompt forbids weakening tests, rolling back the update, widening semver ranges, major bumps, and dependency add/remove/lockfile edits — but the prompt is guidance, not the control. The enforceable control is `verify_no_mandate_violation`: after the loop, any change to a `package.json` dependency specifier (widened range, major bump, added/removed dependency) terminates the run `failed` / `needs_review` / `MANDATE_VIOLATION` and **blocks PR creation**. This mirrors the fleet's "explicitness over inference" and "human review is the real gate" posture — the model may propose, but a deterministic check disposes.

## 7. Data & Database Guidelines

**Canonical schema and seed (issue #115 / S-102 — Supabase CLI migrations).** The schema is now a Supabase CLI migration: [`supabase/migrations/20260902200101_initial_schema.sql`](../supabase/migrations/20260902200101_initial_schema.sql) is the canonical, executed source of truth (full schema: tables, enums, indexes, view, reaper function, RLS — byte-identical to the former reference DDL, including `pg_cron` and the `reap-stale-runs` schedule), and the canonical seed is [`supabase/seed.sql`](../supabase/seed.sql) (idempotent via `on conflict`), applied by `supabase db reset`. Local project config lives in `supabase/config.toml`.

The former reference files [`docs/reference/001_schema.sql`](reference/001_schema.sql) and [`docs/reference/002_seed.sql`](reference/002_seed.sql) are **no longer canonical** — they are now zero-DDL pointer stubs kept only so existing Markdown links still resolve. Do not add DDL/seed SQL to them; edit the migration (or add a new timestamped migration) and `supabase/seed.sql` instead.

**Entities and relationships:**

```
github_installations 1──n repositories
                     1──n runs
agents               1──n runs
repositories         1──n runs
runs                 1──n run_steps
                     1──n run_events
                     1──n run_artifacts
run_steps            1──n run_events
```

**Main tables:**

| Table | Purpose | Design notes |
|---|---|---|
| `github_installations` | One row in v1. Not multi-tenancy — it is a requirement of the GitHub App token flow (token is issued per installation) | `private_key_secret_arn`, never the key |
| `repositories` | Repos enabled to run agents | Unique `(installation_id, full_name)`; `archived_at` is soft delete that preserves historical runs |
| `agents` | Catalog of configured agents | `params_schema` (JSON Schema) does not include the repo; `max_runtime_seconds` **must** reflect the real timeout configured in AgentCore |
| `runs` | One row per execution, `id` generated by the front-end (D1) | Snapshot of timeouts and params; constraint `chk_runs_terminal_outcome` requires non-null `outcome` when `status = succeeded` |
| `run_steps` | Named phases within an execution (`checkout`, `npm_audit`, `llm_fix`, `test`, `open_pr`) | `id` generated by the agent SDK (uuid4), not the database — avoids a read round-trip to associate events to the step |
| `run_events` | The log, as rows, not a blob (D4) | `seq` is monotonic, assigned by the agent, not the database — with buffering, arrival order is not emission order; Realtime enabled, filtered by `run_id` |
| `run_artifacts` | Execution artifacts (`pull_request`, `audit_report`, `diff`, `file`) | The generated PR lives here, not buried in `result jsonb`, so it can be rendered in the list without parsing JSON |

**State machine for `runs.status`:**

```
queued ──▶ running ──▶ succeeded | failed | canceled
   │                       ▲
   │                       └── timed_out        (reaper)
   └────────────────────────── failed_to_start  (reaper)
```

**Key indexes:** `(agent_id, created_at desc)`, `(repository_id, created_at desc)`, partial on `status in ('queued','running')` for the reaper, partial unique on `idempotency_key`.

**Stale execution detection** (see also §3 — two layers): two thresholds on two clocks.

| Condition | New status |
|---|---|
| `status = running` and `now() > started_at + max_runtime_seconds + grace_seconds` | `timed_out` |
| `status = queued` and `now() > queued_at + start_timeout_seconds` | `failed_to_start` |

**Current state (issue #94, ADR-004).** The schedule is **active**, not pending: the canonical
migration ([`supabase/migrations/20260902200101_initial_schema.sql`](../supabase/migrations/20260902200101_initial_schema.sql))
carries `create extension if not exists pg_cron;` and
`select cron.schedule('reap-stale-runs', '* * * * *', $$select reap_stale_runs()$$);` uncommented at
its tail (the extension still has to be enabled with sufficient privilege, so the Supabase-dashboard
note is retained). Each reaped run gets `error_code = RUNTIME_TIMEOUT` or `START_TIMEOUT` plus one
`run_events` row at `max(seq)+1` with `level = error`, `data.reaped_by = reap_stale_runs`, and
`data.reason`. As of issue #99 the function also **closes any open `run_steps`** for a reaped run
(sets `status = 'failed'`, `finished_at = now()`, and an attributing `error_message`), in symmetry
with the agent's own failure path (§8) — so a reaped run no longer leaves an in-flight step pinned
`running`. One caveat remains: because the thresholds mirror AgentCore's own limits, a container that
dies early still reads `running` until its boundary (~61 minutes in the observed case — see §18).
Operator procedures and the verification
evidence are in [`runbooks/issue-94-reaper-verification.md`](runbooks/issue-94-reaper-verification.md).

`last_heartbeat_at` is declared in the schema but not used for detection in v1 — it comes into play only if agents appear that hang well below their timeout (backlog).

**Retention (declared risk, not resolved in v1 — R3).** `run_events` will be the largest table by two orders of magnitude. Policy pending: events older than 90 days get collapsed to an artifact in Supabase Storage and the rows are purged.

**Separate environments (R7).** Local development invokes real AgentCore; to avoid mixing test runs with production runs, the recommended exit is a second Supabase project for development (free tier is sufficient, same schema).

**Panel read boundary — `service_role` SELECT grant asymmetry (issue #117 / S-104).** The panel
reads server-side with the `service_role` key (§5, D15). The canonical migration
([`supabase/migrations/20260902200101_initial_schema.sql`](../supabase/migrations/20260902200101_initial_schema.sql))
grants **no** explicit `SELECT` to `service_role`; the **live** Supabase project carries the
platform-default table privileges that `service_role` receives automatically (confirmed by the
S-115 baseline diff, [`runbooks/issue-115-baseline-adoption.md`](runbooks/issue-115-baseline-adoption.md)),
so production reads work. A fresh local database from `supabase db reset` does **not** reproduce
those platform grants, so the Layer 2.5 `queries` integration test applies the grant locally in
setup — scoped to `service_role` **only**, never `anon`, so RLS deny-all (D11) is preserved and the
anon deny-all test remains a true test. Keeping the platform-managed grants **out** of the canonical
migration is deliberate (the "don't tamper with platform-managed grants" decision from S-115), so no
schema change shipped in S-104. Consequence to record honestly: the **production read path is proven
by inference** (the documented platform default), **not by a live assertion** — a live service-role
smoke read against the hosted project is a suggested future addition, routed to `product-engineer`
for a later deploy story (do **not** move the grants into the migration).

**Panel read boundary — PostgREST `max_rows` vs the SD11 event cap (issue #117 / S-104).** PostgREST
enforces a per-request `max_rows` ceiling (1000 in this project's `supabase/config.toml`), which is
below the SD11 initial `run_events` read cap of 2000. A single `.limit(2000)` would silently return
only 1000 rows, so `getRunEvents` pages internally with `.range()` in ≤1000-row chunks (newest-first
by `seq`, re-sorted ascending for display) until it reaches the cap or the stream is exhausted. This
honors the SD11 contract without changing platform config; the SSE relay (S-110) streams the tail
beyond the cap.

## 8. Integration Methods

**AWS Bedrock AgentCore.** The front-end invokes `InvokeAgentRuntime` in a fire-and-forget manner from the route handler (see §4). There is no automatic retry on the front-end side for startup failures — it marks `failed_to_start` and stops there; the reaper is the safety net for the case where the invocation was accepted but the container never reported.

**Invocation payload contract — repeated `prompt`-wrapper unwrapping (issue #97).** AgentCore delivers the JSON payload inside a `prompt` key as a JSON string. Historically this was a single wrap. As of `agentcore` CLI ≥ 0.28.0 the CLI treats its invoke argument *as* the prompt and wraps it itself, so an already-wrapped argument arrives **double-wrapped**. The agent entrypoint (`main.unwrap_payload`) therefore unwraps **repeatedly** rather than once: it keeps stripping while the current value is a dict whose *only* key is a string `prompt` that parses to a JSON dict (`_is_lone_prompt_wrapper`), bounded by `_MAX_UNWRAP_DEPTH = 16` as a defensive guard. Both the bare inner JSON and the pre-wrapped form are accepted. The loop never over-unwraps a legitimate inner payload that carries its own `prompt` field or sibling keys, and stops on a non-string `prompt` or a `prompt` string that does not parse to a JSON dict. This is a tolerance widening, not a contract change: the required fields (`run_id`, `repository_org`, `repository_name`) and the `INVALID_PARAMS` failure mode are unchanged. When a payload is still wrapper-only after unwrapping — the tell-tale of a double-wrap that could not be resolved — a pure `classify_invalid_payload` helper drives a distinct "appears double-wrapped" diagnostic log line while `error_code` remains `INVALID_PARAMS` (no new error code, no schema change). See [ADR-005](adr/ADR-005-repeated-prompt-unwrap-and-diagnostic.md).

**Panel invocation payload — bare JSON, no `prompt` wrapper (OQ2, settled 2026-09-06).** The panel
sends the **bare inner JSON** as the `InvokeAgentRuntime` payload
(`panel/lib/aws/invoke.ts`: `JSON.stringify(buildAgentPayload(...))` →
`{run_id, repository_org, repository_name, base_branch, params}`); it adds **no** `prompt` wrapper,
and must not. This was the open question OQ2 carried through Wave 4 and is now closed by live
observation against the deployed runtime: a bare-sent payload reached `running` and entered the
pipeline (run `a7203345-…`, `running` at 20:12:31 UTC, `GitHub credentials resolved` at 20:12:32),
and a deliberately malformed bare payload was rejected `INVALID_PARAMS` with the
`missing_fields` classification (session `209f483b-…`) — not the `wrapper_only`
"appears double-wrapped" branch, which a 12-hour log sweep shows has **never** fired.

Honest limit on that evidence: the agent never logs the raw payload (by design), and the shape
cannot be inferred from outside because `unwrap_payload` is *total* — bare, single-wrapped, and
double-wrapped inputs all resolve to the same result, so every externally observable outcome is
identical. What is established is the property that matters operationally: the panel's bare payload
is accepted end to end. The repeated-unwrap tolerance above remains necessary for the
`agentcore` **CLI** path (the subject of #97), not for the panel's SDK path. Verification procedure
and evidence: [`runbooks/issue-89-live-verification.md`](runbooks/issue-89-live-verification.md).

**Agent reporting contract ([`agent_reporter.py`](reference/agent_reporter.py), D13).** Single file copied to each agent repo, no external dependencies (`urllib`, not `supabase-py` or `httpx`). Hybrid interface (D14):

- **Standard `logging.Handler`**, attached to the root logger — captures noise from third-party libraries (`boto3`, `httpx`), which is exactly what you want to see when something fails.
- **Explicit lifecycle API** (`RunReporter.from_env()`, `run.step(...)`, `run.succeed(...)`, `run.fail(...)`, `run.artifact(...)`) — the lifecycle does not fit naturally in a `logger.info()`.

**Long-step keep-alive + timeout-clock invariant (issue #98).** The `@app.entrypoint`
handler is an async generator that historically yielded only at terminal points, so during a
long blocking step (notably `pnpm test` inside `validate`) the AgentCore response stream was
idle. Once idle past `idleRuntimeSessionTimeout`, AgentCore reclaimed the container before the
agent could write a terminal status — the run sat `running` until the reaper marked it
`timed_out` (root cause confirmed from CloudWatch on run `f63ac9f3-…`; the container log ended
cleanly on the last `update`-step line with no exception/OOM/terminal line). The fix keeps the
stream alive: `validate` and `llm_fix` run under `heartbeat.run_with_heartbeat`, which executes
the blocking step in a worker thread and **live-yields lightweight heartbeat chunks** every
`HEARTBEAT_INTERVAL` seconds. Heartbeat chunks (`{"heartbeat": {...}}`) are structurally
distinct from the terminal result payload (the existing `event.contentBlockDelta.delta.text`
shape), the terminal payload is always the last chunk emitted, and a consumer reads it with
`heartbeat.read_terminal_payload` (heartbeats ignored). The reusable logic lives in
`heartbeat.py`, **not** in the vendored `agent_reporter.py` (D13 — the copy must not diverge).

The four timeout "clocks" MUST stay mutually consistent so no inner bound can outlive an outer
one; `config.assert_clock_invariant()` enforces this at entrypoint start (fail-fast with
`ClockConsistencyError` rather than a silent mid-step death — an enforceable rule recorded in
[ADR-006](adr/ADR-006-long-step-keepalive-and-clock-invariant.md)), and a unit test asserts the
shipped constants satisfy it:

```
TOOL_COMMAND_TIMEOUT (180)  <=  TEST_TIMEOUT (600)  <=  IDLE_SESSION_TIMEOUT (900)
                            <=  MAX_LIFETIME (3600)  <=  REAPER_THRESHOLD_SECONDS (3720)
    and   0 < HEARTBEAT_INTERVAL (120)  <=  IDLE_SESSION_TIMEOUT / 2
```

`IDLE_SESSION_TIMEOUT`/`MAX_LIFETIME` mirror `agentcore/agentcore.json`
`lifecycleConfiguration` (`idleRuntimeSessionTimeout` was raised 300 → 900 for this fix — a
runtime redeploy is required, see the pending-manual-config runbook), and
`REAPER_THRESHOLD_SECONDS` mirrors the Supabase run snapshot (`max_runtime_seconds` +
`grace_seconds`). The reaper remains the **outer** backstop, never a competitor to a
legitimately-running container.

**`start_timeout_seconds` is a queue clock, not `idleRuntimeSessionTimeout` (issue #116 / S-103).**
The seed formerly asserted that `agents.start_timeout_seconds (300)` "MUST equal
`idleRuntimeSessionTimeout`". That equivalence was never correct and is now removed. The two measure
different failures on different clocks: `start_timeout_seconds` is the **queue clock** (`queued_at`-based,
D9) — how long an *accepted* invocation may sit before the agent reports a *start*, after which the
reaper marks it `failed_to_start` (§7 table, row 2). `idleRuntimeSessionTimeout` is an **output-idle
clock** — how long AgentCore tolerates a *running* container producing no stream output before it
reclaims it (the issue #98 clock, raised 300 → 900). Issue #98 raising the idle clock to 900 exposed
the stale comment but did not create the mismatch. The resolution is **direction A: correct the
comment, keep the value at 300** — the queue clock has no reason to track the idle clock, so no value
change and therefore no change to the four-clock invariant above. Recorded in `supabase/seed.sql`
block 3.

**Abrupt-termination backstop (issue #98, AC6).** `RunReporter.__exit__` guarantees a terminal
write on any *normal* Python exit, but not on an abrupt kill. `signal_backstop.py` adds a
best-effort SIGTERM handler that marks the active run `failed / SIGNAL_TERMINATION` (message
secret-scrubbed, handler never raises) so an interceptable stop does not wait for the reaper. A
true **SIGKILL / OOM cannot be intercepted** by any process — that path is by design covered
only by the pg_cron reaper.

The agent authenticates to PostgREST directly (D15) using the Supabase service role key fetched from AWS Secrets Manager at startup — not from an env var.

Behavioral properties:

| Property | Behavior |
|---|---|
| Write buffer (D5) | Inline flush every 50 events or 2 seconds; forced at step boundary open/close and on termination. No background thread — async worker adds queue, `join`, and `atexit` for ~200 ms |
| Retries | 3 with exponential backoff. 4xx are not retried (contract error). If exhausted, the payload is written to stderr (lands in CloudWatch) and the agent continues — reporting never kills the agent |
| Agent failure | The context manager marks `failed` with full traceback before re-raising; open steps are closed as `failed` |
| Exit without `succeed()` | Closes with `outcome = not_applicable` to avoid leaving the run dangling |
| Zero-row `start()` (issue #100) | `start()` issues its `running` PATCH with `Prefer: count=exact` and reads `Content-Range`. A **confirmed** zero-row match (no `queued` row was inserted — D1) logs a loud stderr warning naming issue #100, then continues — reporting never kills the agent, and an unknown count (request failed / header absent) does **not** warn, to avoid false alarms |
| Truncation | Messages to 8 KB |
| Transport change | Isolated to the `_SupabaseClient` class (~40 lines) — no generic transport abstraction because today it does not pay for itself |

**Reaper mirrors the agent's step-closure (issue #99).** The "Agent failure" row above closes open
steps as `failed` from the *agent* side. The `pg_cron` reaper is the *database*-side counterpart for
runs the agent never closed itself: on both its branches (`timed_out` and `failed_to_start`),
`reap_stale_runs()` now runs `update run_steps set status='failed', finished_at=now(), error_message=<reaper attribution> where run_id=<run> and status in ('running','pending')`
after materializing the run-level terminal state. `failed` reuses the existing `step_status` enum
value (no new enum value, no migration), and the predicate leaves already-terminal steps
(`succeeded`/`failed`/`skipped`) untouched. A `failed_to_start` run normally has no steps; the update
is a safe 0-row no-op in that case. This closes the orphan-step defect from §7/§18 and removes the
"perpetually pulsing step inside a terminal run" the Phase 2 Run Detail panel (`DESIGN.md` §5.3) would
otherwise have to special-case.

**Per-execution write volume (R5, distinct from R3 which is growth over time).** Not actively mitigated in v1 — the chosen approach is "evaluate later" if an actual agent evidences the problem (e.g., one that tails builds or long test runs). First lever if it occurs: raise the minimum captured log level (`INFO+` instead of `DEBUG+`).

**GitHub App.** The agent reads the private key and `installation_id` from Secrets Manager and issues a short-lived installation token to clone the repo and open the PR. Automatic repo sync from the GitHub App is backlog — v1 uses the manual seed ([`supabase/seed.sql`](../supabase/seed.sql)).

## 9. Code Organization & Structure

Artifacts already defined at design level (see specification §14):

| File/directory | Role |
|---|---|
| [`supabase/migrations/20260902200101_initial_schema.sql`](../supabase/migrations/20260902200101_initial_schema.sql) | **Canonical** schema (Supabase CLI migration, issue #115 / S-102): full DDL — tables, enums, indexes, `v_runs` view, `reap_stale_runs()` function, RLS, `pg_cron` + `reap-stale-runs` schedule |
| [`supabase/seed.sql`](../supabase/seed.sql) | **Canonical** idempotent seed applied by `supabase db reset`: installation, repo list, `dependency-update` agent |
| `supabase/config.toml` | Local Supabase project config (issue #115 / S-102) |
| [`docs/reference/001_schema.sql`](reference/001_schema.sql) / [`docs/reference/002_seed.sql`](reference/002_seed.sql) | **Non-canonical pointer stubs** (issue #115 / S-102). Zero-DDL; kept only so existing Markdown links resolve — edit the migration + `supabase/seed.sql`, never these |
| [`agent_reporter.py`](reference/agent_reporter.py) | Reporting SDK, copied to each agent repo |
| [`docs/reference/credentials.ts`](reference/credentials.ts) | **Non-canonical pointer stub** (issue #124 / S-111). Zero-code; the AWS credential provider was adopted into the panel (`panel/lib/aws/credentials.ts`) with the SD9/F5 corrections. Kept only so existing Markdown links resolve — edit the panel module, never this |
| `panel/lib/aws/{credentials,errors,invoke}.ts` | **Canonical** AWS credential + invocation path (issue #124 / S-111): `credentials.ts` (two-branch provider — Fly OIDC → STS `AssumeRoleWithWebIdentity` / local `fromNodeProviderChain()` — with in-memory cache + single-flight + `credentialSource()`), `errors.ts` (`FlyOidcShapeError`/`CredentialsUnavailableError` 500, `InvocationFailedError` 502), `invoke.ts` (`InvokeAgentRuntime` wrapper vs `agents.runtime_arn` + `runtime_qualifier`). No Next.js imports; never logs token/STS response/credentials |
| `panel/` front-end | Next.js 15 (App Router) package under `panel/`, the primary member of the repo-root pnpm workspace — scaffolded in Phase 2 (issue #114 / S-101). `app/layout.tsx` (DESIGN §1.2 Inter `<link>`, imports `styles/tokens.css` + `styles/globals.css`) + `app/page.tsx`. As of issue #117 / S-104 the **server-side Supabase read boundary** exists: `lib/supabase/server.ts` (`createServerClient` per-request factory + `readSupabaseEnv` fail-fast validation, `import "server-only"` hard guard), `lib/supabase/queries.ts` (eight typed read helpers; `getRunEvents` pages internally with `.range()` to honor the SD11 2000-event cap under PostgREST `max_rows=1000` — see §7), `lib/supabase/types.ts` (hand-written row types for the six read objects), `lib/supabase/errors.ts` (`DATABASE_ERROR` — Postgres code logged, never returned to the client), `lib/supabase/route-config.ts` (canonical `force-dynamic`/`revalidate=0`/`fetchCache` values), and `lib/domain/status.ts` (`effectiveStatus`, the pure TS mirror of `v_runs.effective_status` (SD4), pinned to the SQL by a live Layer 2.5 parity test). As of issue #118 / S-105 the **Nocturne presentational layer** exists (presentational + pure, no schema/data/API change): `styles/tokens.css` (every DESIGN §2 token incl. the four SD10 `--st-*` status colors, imported in `app/layout.tsx`), `styles/globals.css` (`pulse`/`spin`/`rise` keyframes, the `:focus-visible` accent ring, and a reduced-motion guard — DESIGN §6), `lib/format.ts` (the DESIGN §7 data formatters — clock, relative, durations, run-id, counts, legends), the twelve DESIGN §11.2 primitive components under `components/` (`KLabel`, `Tag`, `Button`, `Input`, `StatusDot`, `StatusPill`, `Toggle`, `Breadcrumb`, `NavItem`, `StatusBar`, `RunStrip`, `LogLine`, each with a token-only `.module.css`) plus `components/status-meta.ts` and `components/icons.tsx` (Phosphor via `@phosphor-icons/react/ssr` on `currentColor`, DESIGN §10), and `app/dev/gallery/*` (a dev-only variant gallery that 404s in production). `app/dev/agent-count/page.tsx` is a manual-verification server route. As of issue #124 / S-111 the **AWS credential + invocation path** exists under `lib/aws/`: `credentials.ts` (the two-branch provider adopted from `docs/reference/credentials.ts` with the SD9/F5 corrections — Fly OIDC → STS `AssumeRoleWithWebIdentity` `DurationSeconds:900` / local `fromNodeProviderChain()`, in-memory cache + 60 s refresh margin + single-flight, `credentialSource()` diagnostic, `AGENT_RUNTIME_ROLE_ARN` fail-fast), `errors.ts` (the §13 taxonomy — `FlyOidcShapeError`/`CredentialsUnavailableError` 500, `InvocationFailedError` 502), and `invoke.ts` (the `InvokeAgentRuntime` wrapper logging `credentialSource()` on every invoke, consumed by S-112); the reference `credentials.ts` is now a MOVED pointer stub. As of issue #119 / S-106 the **app shell** exists (presentational + pure, no schema/data/API change): `components/shell/{AppShell,Sidebar,TopBar,DisabledNavItem}.tsx` each with a token-only `.module.css` — a 212px/52px collapsible sidebar, a 38px top bar with a breadcrumb slot (plus a forward-looking optional `actions` slot), and a content region that owns its own scroll under `height:100dvh;overflow:hidden` so the page never scrolls (DESIGN §4.1); `AppShell` is a `"use client"` component that owns the collapse state and wraps the authenticated routes via `app/(panel)/layout.tsx` (as of issue #157 / S-118 — see the route-group note at the end of this row; before S-118 it wrapped `app/page.tsx` via the root `app/layout.tsx`). `lib/ui/sidebar-state.ts` reads/writes the collapse preference in `localStorage` (`SIDEBAR_STORAGE_KEY = "panel.sidebar.collapsed"`) over a closed `{collapsed,expanded}` two-literal vocabulary defaulting to expanded (`DEFAULT_COLLAPSED = false`), returning the default for every failure mode (absent/throwing storage, unrecognized value) and never throwing. `lib/ui/shortcuts.ts` holds pure `KeyboardEvent` predicates — `isSidebarToggleShortcut` (Cmd+\ on mac, Ctrl+\ elsewhere, primary modifier only) and `isTypingTarget` (input/textarea/select/contenteditable guard). **Hydration contract (the S-106 design decision):** the shell renders the fixed server default (expanded) and reconciles the stored collapse state in a mount effect — never during render — so server and client markup match and no hydration mismatch occurs. Only Agents is an enabled nav destination; All runs / Repositories / Settings / System health render as non-link `DisabledNavItem` spans (`aria-disabled`, "not available in this phase", not focusable) per PRD §10. As of issue #120 / S-107 the **agents dashboard** exists at `/` (read-only Phase 2 UI — no schema/data/API change): `app/page.tsx` is a thin async **server component** that declares its route-segment config **inline** (`dynamic="force-dynamic"`, `revalidate=0`, `fetchCache="force-no-store"` — Next.js ignores re-exported segment config, S-104 audit D4 / §12, and a cached run list is exactly the staleness FR11a prevents), reads once via `getDashboardData`, shapes with `buildAgentSummaries`, and renders `components/dashboard/DashboardClient.tsx`. `lib/domain/dashboard.ts` is the pure shaper: `buildAgentSummaries` derives **every** status it emits — each `recentRuns` entry, the `lastRun`, and every `breakdown`/`legend` count — through `effectiveStatus` (never the raw `status` column) so a count can never disagree with the pill a variant renders (FR11a / CT-1), importing the SD4 derivation from `lib/domain/status.ts` and never restating the threshold arithmetic (CT-3); `matchesQuery`/`filterAgentSummaries` are the transient name+slug filter predicate (plain `String.includes`, never a constructed `RegExp`, so a stray `[` matches literally and cannot throw — EC-25). `lib/ui/density-state.ts` persists the chosen density to `localStorage` (`DENSITY_STORAGE_KEY = "panel.dashboard.density"`) over a closed `{dense,cards,ledger}` three-literal vocabulary defaulting to `dense` (variant 1a), returning the default for every failure mode (absent/throwing storage, unrecognized value) and never throwing — the same hydration contract as the sidebar (fixed server default reconciled from storage after mount). `components/dashboard/*` holds the three DESIGN §5.1 variants (`DenseRows` default, `AgentCards` with the 24-bar `RunStrip`, keyboard-first `Ledger`), `DensityToggle`, `AgentFilter` (transient header filter over name+slug, **never persisted**), `DashboardClient` (the `"use client"` owner of density + filter state), and the shared `summary-view.ts` presentation helper. `lib/supabase/queries.ts` gains `getDashboardData` — **two reads, never N+1** (enabled agents, then their runs; CT-7), paged with `.range()` below the PostgREST `max_rows=1000` ceiling so per-agent counts stay true beyond 1000 runs (CT-8) and returning `{agents:[],runs:[]}` for an empty fleet, never null. `components/Input.tsx` gains a small `forwardRef` addition (filter-input focus). **Recorded scope decision:** the DESIGN §5.1 time-range chips (7d/30d/all) were **not** built — "all" only, because a client-side window would misreport counts and a server window needs a `searchParams` write that reintroduces the forbidden toggle refetch (routed to `product-engineer` for DESIGN §5.1 write-back, not made here). As of issue #121 / S-108 the **agent run-history screen** exists at `/agents/[slug]` (read-only Phase 2 UI — no schema/data/API change): `app/agents/[slug]/page.tsx` is a thin async **server component** that declares its route-segment config **inline** (`dynamic="force-dynamic"`, `revalidate=0`, `fetchCache="force-no-store"` — S-104 audit D4 / §12; a cached run list is exactly the staleness FR11a prevents since a run's derived status changes second-to-second), reads all runs newest-first from `v_runs`, computes the params count off `params_schema`, and **404s on an unknown OR a disabled slug** (a disabled agent is not-found, never an empty list). `lib/domain/run-row.ts` is the pure, **clock-injected** row shaper + agent-header metric derivations: `buildRunRow`/`buildRunRows` project each `v_runs` row into the table row, and `buildAgentHeader` derives p50 duration (via `lowerMedian` — a real observed run's time, never a synthetic midpoint), whole-percent success rate, and run count; **every status this module emits — each row's status AND the status feeding the success-rate metric — is derived through the shared `lib/domain/status.ts` `effectiveStatus`** (never the raw `status` column), so a stale `running` run presents `timed_out` and the header rate can never disagree with the rows below it (FR11a), importing the SD4 derivation and never restating the threshold arithmetic (CT-3 analogue); `shouldRunHistory404` is the pure 404 guard, and `outcomeLabel` maps outcomes to the uppercase §8.2 tags (incl. `not_applicable` → `N/A`). `lib/supabase/queries.ts` gains two read helpers: `getAllRunsByAgentSlug` (the full history — unfiltered, unpaginated, newest-first from `v_runs`, paged internally with `.range()` below the PostgREST `max_rows` ceiling so the count and metrics stay true beyond 1,000 runs) and `getStepProgressForRuns` (a **single grouped read, never N+1**, reading only `run_id,status` for every step of the given run ids and folding them into a `run_id → {done,total}` map where `done` counts steps that have left `pending`, feeding the `n/m` Steps column; returns an empty map for an empty id list). `components/runs/{AgentHeader,RunHistoryTable,RunHistoryRow}.tsx` render **semantic `<table>` markup** (spec §10, not a div grid) with token-only CSS Modules on the DESIGN §4.4 grid (`118px 122px minmax(0,1fr) 96px 78px 104px 30px`) and the §5.2 fields (status pill, outcome tag, repository + branch + PR, duration, steps `n/m`, relative start, chevron). `docs/runbooks/issue-121-ac10-reaper-paused.md` records the AC10 reaper-paused verification procedure (local stack only) with recorded evidence. **Recorded deferrals** (so they aren't mistaken for missing scope): filters / repo chips / search / pagination + the "live" indicator (PRD §10), the inline PR link / error-detail in the repo cell (S-109/S-110), the forward link to `/runs/[id]` and the enabled Invoke action (S-113 — Invoke renders disabled until that route lands), and the 1024px pixel-geometry check (Playwright, S-114). As of issue #122 / S-109 the **run-detail screen** exists at `/runs/[id]` (read-only Phase 2 UI — no schema/data/API change): `app/runs/[id]/page.tsx` (+ `page.module.css`) is a thin async **server component** that declares its route-segment config **inline** (`dynamic="force-dynamic"`, `revalidate=0`, `fetchCache="force-no-store"` — S-104 audit D4 / §12; a run's derived status and its log tail change second-to-second, exactly the staleness FR11a prevents), reads the run + steps + events + artifacts (reusing the S-104 `getRunById`/`getRunSteps`/`getRunEvents`/`getRunArtifacts`), calls `notFound()` (404) on an unknown id, and exposes a `"use server"` `loadEarlier` server action for the log window. Three new pure domain modules under `lib/domain/`: `artifact-url.ts` (`isSafeArtifactUrl` — the mandatory security guard #5, spec §12/A10 — a total, never-throwing WHATWG-`URL` check that links only well-formed `https:` URLs, so an agent-written `javascript:`/`data:`/`http:`/relative `run_artifacts.url` renders inert), `log-window.ts` (the SD11 most-recent-`LOG_WINDOW_SIZE`=2,000 window selector `selectRecentWindow` — sorts by `seq` first because agent buffering means arrival order is not emission order, D5 — plus the contiguous, gap-free/overlap-free `priorWindowRange` load-earlier math), and `run-detail.ts` (`selectBanner` — the DESIGN §8.3 terminal-state banner selection, total over every status, only `timed_out`/`failed_to_start` produce one; `buildLogLines` — projects events into the 4-column LogLine grid, message carried **verbatim, never truncated** §7.5; `buildSummary` — derives the summary status through the shared `lib/domain/status.ts` `effectiveStatus`, never the raw column, so the summary pill and banner agree with the row screens). `components/run-detail/{RunSummary,ArtifactLinks,LogViewer,StateBanner}.tsx` each with a token-only CSS Module: `ArtifactLinks` renders artifacts on **failed** runs too (AC14; https-only via `isSafeArtifactUrl` + `rel="noopener noreferrer"`), `LogViewer` is the `"use client"` 4-column `LogLine` grid ordered by `seq` that renders each message as an **inert text node — never `dangerouslySetInnerHTML`** (security guard #6), is `aria-live="polite"`, drives "load earlier", and leaves a stable `data-sse-mount="run-log"` hook for the S-110 live-tail relay, and `StateBanner` renders the DESIGN §8.3 `timed_out`/`failed_to_start` banners carrying the reaper's explanatory `error_message`. `lib/supabase/queries.ts` gains `getRunEventsInRange` (the load-earlier windowed read, paged with `.range()` below the PostgREST `max_rows` ceiling). Full-height layout per DESIGN §4.2 (no outer scroll — the log region owns the scroll). **Recorded deferrals** (so they aren't mistaken for missing scope): the 1024px pixel-geometry check (Playwright, S-114). As of issue #123 / S-110 the **SSE live-tail relay** exists (read-only Phase 2 UI/relay — no schema/data/API-table change; migration is a documented N/A opt-out): `app/api/runs/[id]/events/stream/route.ts` is the panel's **first `app/api/**` route handler** — `GET /api/runs/[id]/events/stream`, `text/event-stream`, an `after_seq` cursor coerced to a non-negative integer (else 0), route-segment config declared **inline** (`dynamic="force-dynamic"`, `revalidate=0`, `fetchCache="force-no-store"`, `runtime="nodejs"` — S-104 audit D4 / §12; a stream must never be cached or statically rendered) that **backfills then subscribes** via a **server-side** Supabase Realtime subscription so the browser holds no Supabase credentials (SD2), emits a heartbeat every 15 s, closes with a `{reason}` frame on terminal state or request abort, and writes the SR5 open/close structured-log pair keyed by `run_id` + last `seq` (§13). The sequencing/serialization is a pure, dependency-injected layer under `lib/sse/`: `relay.ts` (the Next-/Supabase-free engine that owns the SD6 contract — backfill `seq > after_seq` first, then subscribe, then drop any pushed row at/below the highest already sent — plus the SR5 lifecycle logging and single unsubscribe on terminal/abort), `cursor.ts` (the pure `SeqCursor` + `dedupeAndOrder` gap-free-by-`seq` reducer — ordering strictly by the monotonic agent-assigned `seq` because buffering means arrival order ≠ emission order, D5), `serialize.ts` (the newline-safe SSE frame serializer/parser over the four event types `event`/`run`/`heartbeat`/`closed`, JSON-encoded so a payload can never forge the blank-line frame terminator), and `autoscroll.ts` (the pure DESIGN §6.6 "within 24px of bottom" predicate — inclusive at 24px). `lib/hooks/useRunStream.ts` is the `"use client"` `EventSource` hook that appends `event` frames (client-side dedupe by `seq` too, defensively), stops reconnecting once the server sends `closed`, and on an *unexpected* drop reconnects with the highest rendered `seq` as `after_seq` so no line is lost. As of issue #160 / S-121 the hook also distinguishes a **terminal authorization stop** from a genuine drop: it tracks whether the *current* attempt ever opened (`onopen`, or the first `event`/`run` frame), and an `onerror` on an attempt that **never opened** with `readyState === CLOSED` is treated as terminal — it stops, does **not** reconnect, schedules no timer, and exposes `authStopped: true` (whereas a previously-opened drop keeps the S-110 reconnect, `SeqCursor` dedupe intact). This is what stops an expired session from producing an infinite `401` reconnect loop, and it **settles spec OQ3**: the S-117 SSE gate returns a plain `401` *before* the `text/event-stream` response opens (an `EventSource` cannot send headers), so the "never opened + CLOSED" signal — not a `closed{reason:"unauthorized"}` frame — is the client's authoritative unauthorized tell. `components/run-detail/{LiveLogViewer,LiveTailButton}.tsx` add the two live behaviors S-109 deferred: **AC5** — the displayed status is re-derived through the shared `lib/domain/status.ts` `effectiveStatus` (SD4) at the current instant, so a raw Realtime `running` push on an expired run still reads `timed_out` (the derived status is never overwritten by the wire status); and **AC6/DESIGN §6.6** — new lines follow the scroll only while within 24px of the bottom, scrolling up pauses and the `LiveTailButton` shows the paused state, clicking it resumes and re-pins to the bottom. The live viewer reuses the S-109 `LogLine` primitive and renders each message as an **inert text node — never `dangerouslySetInnerHTML`** (security guard #6). As of issue #160 / S-121 `LiveLogViewer` renders a token-only session-expired notice (`role="alert"`, `LogViewer.module.css` `.sessionExpired`) and drops the live-tail control when the hook reports `authStopped`, so an expired-session tail surfaces a visible "reload to sign in and resume" message instead of silently freezing. `app/runs/[id]/page.tsx` now selects `LiveLogViewer` (when the **effective** status, SD4, is non-terminal) vs the S-109 server-rendered `LogViewer` with "load earlier" (terminal runs — nothing to tail). `lib/supabase/queries.ts` gains `getRunEventsAfterSeq` (the relay backfill — `seq > afterSeq`, ascending, paged with `.range()` below the PostgREST `max_rows` ceiling, **unbounded on the upper end**: an earlier `Number.MAX_SAFE_INTEGER` sentinel ceiling overflowed the `integer` `seq` column, pg 22003, and was caught by the live Layer 2.5 run). **Recorded deferral:** the 1024px pixel-geometry check (Playwright, S-114). Remaining routes and schema-generated forms land in later Phase 2 stories. As of issue #157 / S-118 the authenticated routes named above (`page.tsx`, `agents/**`, `runs/**`, `dev/**`) live under the **`app/(panel)/` route group** (a pure mechanical `git mv`, byte-identical, no behavior/schema/data/API change): a new `app/(panel)/layout.tsx` renders `AppShell`, and the root `app/layout.tsx` no longer wraps `AppShell` — it keeps only `<html>`/`<body>`, the Inter fonts, metadata, and the global CSS imports, so a future public route (e.g. `/login`, S-119) can render outside the shell. The `(panel)` parentheses do not appear in URLs, so every path resolves exactly as before (`/`, `/agents/[slug]`, `/runs/[id]`), and `app/api/**` is unchanged; earlier `app/<page>` references in this row denote the same files now under `app/(panel)/`. As of issue #158 / S-119 the **public login screen** exists at `/login` (the panel's only public route — no schema/data/API-table change; auth state lives in Supabase `auth.*` + cookies, migration is a documented N/A opt-out): `app/login/{page.tsx,layout.tsx,actions.ts,login.module.css}` render **outside** the `(panel)` route group (so no `AppShell`). `page.tsx` is a thin async **server component** with route-segment config declared **inline** (`dynamic="force-dynamic"`, `revalidate=0`, `fetchCache="force-no-store"` — an auth response can carry a refreshed `Set-Cookie`, so it must never be cached, S-104 audit D4 / §12) that redirects an already-authenticated visitor (verified via `getClaims()`, never `getSession()`) to `/`, pre-sanitizes the untrusted `?redirect` via `safeRedirectTarget`, and renders the Nocturne card (§4.1 brand pattern + `LoginForm`); `layout.tsx` is a shell-free wrapper with the page metadata. `actions.ts` is the `"use server"` `signIn` action built on a pure, injectable `resolveSignIn` core (validate fields → `safeRedirectTarget` **before use** → `signInWithPassword` → generic error mapping), redirecting to the sanitized target on success. `components/auth/{LoginForm,PasswordField}.tsx` (+ token-only CSS Modules) reuse the S-105 `Input`/`Button`/`KLabel` primitives: `LoginForm` (`"use client"`) drives the action via `useActionState`, uses `useFormStatus` for the pending/disabled double-submit guard, renders the generic failure message in a `role="alert"` region, and remounts the (uncontrolled) password field on a failed attempt to clear it; `PasswordField` (`"use client"`) is the keyboard-operable `SHOW`/`HIDE` reveal toggle (`aria-pressed`, `aria-controls`, defaults **masked**, `type="button"` so it never submits). It **reuses (does not reimplement)** the S-116 `lib/auth/{redirect,errors}` policy + `lib/supabase/auth-server`, and is protected by the S-117 middleware gate. Security properties preserved: anti-enumeration (unknown-email ≡ wrong-password, one generic message via `classifySignInError`), redirect safety (`safeRedirectTarget` server-side before use), password never stored/logged/echoed, no signup/reset/confirmation/magic-link, and the SD2/SA1 boundary intact (the auth path uses the anon-key client family, never the service-role data client). Sign-out UI lands in S-120. As of issue #159 / S-120 the **sign-out path** exists (no schema/data/API-table change — auth state lives in Supabase `auth.*` + cookies, migration is a documented N/A opt-out): `app/api/auth/logout/route.ts` is the panel's **third `app/api/**` route handler** (after the S-110 SSE stream and the S-112 `POST /api/agents/[slug]/invoke` route; the S-120 code comments say "second", overlooking the S-112 invoke endpoint — the accurate count is three) — `POST /api/auth/logout`, **POST-only** (no `GET` export, so a `GET` is a 405 — a `GET` logout would be CSRF-triggerable), route-segment config (`force-dynamic`/`revalidate=0`/`fetchCache="force-no-store"`/`runtime="nodejs"`) declared **inline** (S-104 audit D4 / §12; a logout response carries cleared `Set-Cookie` and must never be cached), that builds the S-116 cookie-backed anon auth client, calls `signOut` **best-effort/idempotent** (a no-session call is a no-op, any transient error is swallowed), and **always** redirects `302 → /login` (it **never 500s** — ending a session must not be blockable by an auth error). `lib/auth/route-policy.ts` (S-116) reclassifies the **exact** path `/api/auth/logout` as `public` so the S-117 gate short-circuits before auth and the session-ending POST reaches the handler even with an expiring/invalid session; the match is exact (siblings `/api/auth/*` and `/api/auth/logout/extra` stay gated `api`) and POST-only, so no read surface is widened. `components/shell/LogOutItem.tsx` (+ `.module.css`) is the presentational sidebar-footer affordance — a plain `<form method="post" action="/api/auth/logout">` submit button reusing the footer `.toggle` grid (icon + label, icon-only when collapsed, accessible name "Log out" preserved in both states), token-only CSS, no client handler; `components/icons.tsx` maps `LogOutIcon → Phosphor Power` (`/ssr`, DESIGN §10). `components/shell/{Sidebar,AppShell}.tsx` thread an `authenticated` prop and render Log out **between "System health" and "Collapse"**, gated on `authenticated`; `app/(panel)/layout.tsx` (server) computes `authenticated` via the S-116 auth-server client + `getClaims()` (never `getSession()`; fail-closed — any error hides the control) and threads it in. **SD2/SA1 preserved** — the shell performs no auth I/O (the value is a static per-request server prop, so the S-106 hydration contract is untouched), the logout path uses the anon-key auth client family (never the service-role data client), and RLS stays deny-all (D11). React 19, TypeScript strict, ESLint + Prettier, Vitest (unit/component/integration projects) + Playwright config stub. |
| `dependency-update` agent | AgentCore runtime under `agents/dependency-update/` — implemented (Phase 1): deterministic audit→classify→update→validate pipeline, the bounded LLM fix loop (`fix_agent.py`, ADR-001), and the idempotent `open_pr` step with the `pull_request` run artifact (`pull_request.py`, issue #76). Run metrics are computed correctly as of issue #90: `advisories_fixed` is an advisory ID-set diff between the before/after audits (`audit.count_advisories_fixed`), and `packages_changed` is derived from a workspace-aware, recursive lockfile snapshot (`pnpm list -r --depth Infinity` / `npm list --all`) — the previous `in_range` bucket subtraction and `--depth 0` root-only listing both under-reported (0) on monorepos. A single fixed-advisory count feeds both the PR body Security Summary and `runs.metrics.advisories_fixed` so they cannot disagree. Deferred to issue #77: fix-budget test-output artifact, plus deploy + E2E. |

The repository is now a **pnpm workspace** (`pnpm-workspace.yaml`, members `panel` and
`agents/dependency-update/agentcore/cdk`) with a root `package.json` whose canonical scripts
delegate to `panel` via `pnpm --filter`. The front-end folder convention is settled for the
scaffold: the `panel` package follows the Next.js App Router layout (`app/`, with `lib/` and
route directories added as Phase 2 screens land). The repo-root `Makefile` `validate` runs a
Python branch and a JS/TS branch, both of which must pass.

## 10. Design Patterns & Principles

- **Explicitness over inference.** Every observable state in the panel is written explicitly by the agent or the reaper; nothing is derived by parsing free text.
- **YAGNI on transport and infrastructure abstractions.** No pip package for the SDK (D13), no durable queue for invocation (D7), no `agent_versions` table (snapshot in `runs` suffices). These decisions are explicitly revisable if the fleet grows (see backlog).
- **Auditability by snapshot, not by referenced version.** Each `run` is self-contained regarding the parameters and thresholds it ran with, even if the agent's configuration changes later.

## 11. Testing Strategy

The canonical testing contract lives in [`TESTING.md`](../TESTING.md) — this section summarizes the current state for the `dependency-update` agent (Phase 1) and points to it.

- **Framework and layers (Python agent).** `pytest 8.3.5` with branch coverage via `pytest-cov`. Layer markers are auto-applied by directory in `tests/conftest.py`: `tests/unit/` → Layer 1 (`unit`, no I/O/network — all `boto3`/`requests`/`jwt`/`subprocess` boundaries mocked), `tests/component/` → Layer 2 (`component`, mocked externals + temp-dir project fixtures). The aggregate gate is `make validate` (lint + format:check + typecheck + test-cov + audit), enforced in CI (`.github/workflows/ci.yml`) on a Python 3.13 + 3.14 matrix for every push/PR to `main`.
- **LLM fix agent test surface (issue #75).** The escape hatch adds a Layer 1 + Layer 2 surface exercised without invoking a real model: `tests/unit/test_safe_path.py` (workspace-escape guard — traversal, absolute, symlink, null-byte), `tests/unit/test_mandate_check.py` (`verify_no_mandate_violation` add/remove/change/malformed-JSON/missing-file), `tests/unit/test_fix_tools.py` (tool path-safety), and `tests/component/test_fix_agent.py` (5-tool surface, retry-budget exhaustion, early success, `max_attempts=0` → zero model calls, agent-exception resilience) with the Strands `Agent` mocked. `fix_agent.py` reports ~91% line coverage.
- **PR-creation test surface (issue #76).** The `open_pr` step adds a Layer 1 + Layer 2 surface with `git`/`gh` mocked: `tests/unit/test_pr_body.py` (branch naming + conditional PR-body sections, cap-at-30, AI-warning, validation table — 19 tests) and `tests/component/test_pr_creation.py` (idempotency short-circuit, `--body-file` never inline, never-push-to-default, credential-helper push, commit-message contract — 14 tests), plus additions to `tests/component/test_pipeline.py` (token-staleness re-mint). `pull_request.py` reports ~95% line coverage.
- **Run-metric under-report fix test surface (issue #90).** The metric-computation fix adds a Layer 1 + Layer 2 surface: `tests/unit/test_audit.py` gains `TestCountAdvisoriesFixed` (advisory ID-set diff — unknown-bucket fixtures, equal sets, all-fixed, empty-before, no-negative result, dedupe) and monorepo-recursion cases for `_parse_list_json` / `snapshot_lockfile_packages` (workspace + transitive capture, command-shape assertions), backed by new fixtures (`audit_pnpm_before.json`, `audit_pnpm_after.json`, `list_pnpm_monorepo.json`); `tests/component/test_pipeline.py` gains `test_security_summary_uses_id_set_diff_not_bucket_count` asserting the PR body Security Summary and Package Changes agree with the real diff (no contradictory `fixed: 0`). Full suite: **362 tests passing**.
- **Known gaps (tracked, non-blocking).** No Layer 3 product-evaluation/eval harness exists for the LLM path (semantic/groundedness) — the fix-agent *code path* is tested but its output *quality* is not. The `main.py` orchestrator is coverage-excluded, so the req-49→req-50→`open_pr` guard ordering, the PR-before-`MAJOR_UPDATE_REQUIRED` sequencing, and the `pull_request` artifact emission are verified by inspection, not by an automated test. `agent_reporter.py` (buffering/retry/`seq`) has no committed tests. Security-negative coverage of the GitHub App / Supabase auth path in `credentials.py` is largely absent because the token endpoint is mocked. See `TESTING.md` (Coverage, Security-Negative Tests) for the ranked gap analysis.
- **`validate`-step keep-alive test surface (issue #98).** The heartbeat fix adds a Layer 1 + Layer 2 surface exercised without a live runtime: `tests/unit/test_heartbeat.py` (chunk contract, `run_with_heartbeat` short/long/exception/bounded-count, terminal-last property RT-1, consumer parser + fuzz RT-3), `tests/unit/test_clock_invariant.py` (shipped-config consistency, rejection of each violated relation SC-4, heartbeat ≤ idle/2 EC-2, property RT-2), `tests/unit/test_signal_backstop.py` (best-effort SIGTERM report, already-terminal no-op, secret-scrub EC-7, never-raises), and `TestEntrypointHeartbeatWiring` in `tests/component/test_pipeline.py` (a slow mocked `validate` emits ≥1 heartbeat and a single terminal chunk last, SC-3). `heartbeat.py` ~94%, `config.py` ~96% line coverage; `signal_backstop.py`'s signal-registration path is not unit-covered (its reportable core is). Full suite: **416 tests passing**.
- **Front-end (`panel`, Phase 2).** The JS/TS application test package exists (issue #114 / S-101): `panel` uses **Vitest 3.2.4** with config-level `projects` (unit / component / integration) + `@vitest/coverage-v8`, reachable from the repo-root `make validate` JS/TS branch. As of issue #118 / S-105 the `integration` project is pinned single-threaded (a pre-existing S-104-layer shared-DB flake — a harness-only fix, no product-code change). As of issue #127 / S-114 the **Playwright E2E suite is live** — `panel/playwright.config.ts` (retries 0, one worker, headless in CI, `webServer` runs `next dev`) drives a committed suite under `panel/tests/e2e/` (config + `global-setup.ts` + fixtures + `invoke.spec.ts`/`live-tail.spec.ts`/`stale-and-artifact.spec.ts`/`density.spec.ts`/`edge-cases.spec.ts`), no longer a stub. The `agentcore/cdk/` package still has its single CDK synth smoke test under `jest`. See `TESTING.md` for the per-package table and the scenario-to-AC map.
- **Server data-layer test surface (issue #117 / S-104).** The read boundary adds a Layer 1 + Layer 2.5 surface (68 panel tests + a `RUN_BUNDLE_SECRET_TEST=1`-gated bundle test). Layer 1: `tests/unit/status.test.ts` (SD4 truth table — fresh/stale, terminal pass-through, null `started_at`, ±1s boundary, zero/negative grace, unknown-future status), `tests/unit/errors.test.ts` (`DATABASE_ERROR` hides the pg code), `tests/unit/server-env.test.ts` (env fail-fast), `tests/unit/eslint-server-import.test.ts` (SD2 rule fires on a `components/**` client fixture, does **not** fire on a server-context module), and `tests/unit/bundle-secrets.test.ts` (sentinel build + grep of `.next/static/**` — no service-role key in any client chunk, CT-7). Layer 2.5 (Docker-gated, **ran live** for this delivery): `tests/integration/status-parity.test.ts` (pins `effectiveStatus` to `v_runs.effective_status` over a matrix incl. exact-boundary rows), `tests/integration/rls-deny-all.test.ts` (anon reads zero rows from every table **and** `v_runs`, with a non-vacuous seeded-rows guard), and `tests/integration/queries.test.ts` (helper runtime shapes, empty→`[]`, no-repository run, and the >2000-event bounded read proving SD11 is honored under `max_rows=1000`). The `queries` suite applies a local `service_role` SELECT grant in setup (§7 grant-asymmetry note); the standing G2 obligation — CI must start the local stack and treat a skipped parity/deny-all project as a failure — is **RESOLVED as of issue #134 / S-114** (see the S-114 row below): the CI panel job now boots the Supabase stack (`supabase start` + `db reset`) and sets `REQUIRE_LOCAL_DB=1`, which the shared `probeLocalDb` in `panel/tests/integration/db.ts` reads to turn a Docker-gated skip of any Layer 2.5 suite into a hard failure — uniformly across all thirteen suites, so a Layer 2.5 skip can no longer pass CI vacuously.
- **Design-token + primitive test surface (issue #118 / S-105).** The Nocturne presentational layer adds a Layer 1 + Layer 2 (component) surface, all run without a browser via jsdom (12 component suites + 2 unit suites, ~67 S-105 tests). Layer 1 unit: `tests/unit/format.test.ts` (table-driven DESIGN §7 formatters — zero/negative/sub-second durations, exact-minute, far-past relative, run-id casing, count legends) and `tests/unit/token-discipline.test.ts` (a **mechanical gate**, not a review note: rejects any color hex literal and any `font-family:` literal under `components/**` + `styles/globals.css`, with `tokens.css` the single exempt literal home and a vacuity guard requiring ≥12 modules scanned). Layer 2 component: one suite per primitive under `tests/component/*.test.tsx` (`Button` variants×sizes+disabled, `Tag`, `StatusPill`/`StatusDot` across all six statuses incl. `failed_to_start` hollow + running/queued pulse + unknown fallback, `Input`, `Toggle`, `LogLine` full-length 8 KB wrap, `NavItem`, `Breadcrumb`, `StatusBar`, `RunStrip`, `KLabel`). `coverage_gate` **PASS** — 100% stmts/lines/funcs on every S-105 component + `lib/format.ts`. Suite counts: panel **232 passed / 19 skipped** (integration `queries`/`rls-deny-all` Docker-gated), deterministic across five repeats; `make validate` exits 0 (Python 436 + panel 232). Known non-blocking carve-out: bare dimensional `px` is intentionally **not** in the token-discipline gate — the prototype fixes exact pixel dimensions outside the six-step `--space-*` scale (grid tracks, dot/knob diameters, 1–3px radii); documented in the test header and `panel/README.md` (verifier drift D5).
- **AWS credential + invocation test surface (issue #124 / S-111).** The credential path adds a Layer 1 unit surface exercised without a live Fly socket, real STS call, or real AgentCore (36 tests): `tests/unit/credentials.test.ts` (branch detection ×3; the Fly OIDC → `AssumeRoleWithWebIdentity` exchange asserting socketPath/path/method/`aud`/forwarded-token/`DurationSeconds:900`; token extraction accept `value`/`token`+prefer-`value` and reject `{aud}` (F5 — audience value never leaked), `{}`, non-JSON, non-string `value`, array; local branch delegates to the chain with STS untouched; cache 61 s→hit / 59 s→miss; single-flight two-concurrent→1 STS call; STS `AccessDenied`/`InvalidIdentityToken`/network→`CREDENTIALS_UNAVAILABLE` 500; missing-`AGENT_RUNTIME_ROLE_ARN` named startup error; socket-refused/HTTP-500 loud with no silent local fallback; secret-in-logs guard), `tests/unit/aws-errors.test.ts` (pins both codes/statuses and their non-equality), and `tests/unit/invoke.test.ts` (logs `credentialSource()` on every invoke reflecting both branches, never logs payload, 500-vs-502 split). `coverage_gate` **PASS** — `lib/aws` 97.44% stmts. `make validate` green (Python 436 + panel 275). Two design-deferred, pre-flagged partials remain non-blocking: the live Fly-socket shape probe (AC2/OQ1 → S-115) and the route-level "logged on every invoke" + 500/502 route mapping (AC6/G5 → S-112).
- **App-shell test surface (issue #119 / S-106).** The shell adds a Layer 1 + Layer 2 (component) surface, all run without a browser via jsdom (34 S-106 tests). Layer 1 unit: `tests/unit/sidebar-state.test.ts` (9 — the closed `{collapsed,expanded}` vocabulary; unknown/corrupt/empty/absent value → default; throwing `getItem`/`setItem` swallowed; round-trip) and `tests/unit/shortcuts.test.ts` (11 — `isSidebarToggleShortcut` mac Cmd+\ vs other Ctrl+\, primary-modifier-only rejects Cmd+Shift+\ / Ctrl+Alt+\; `isTypingTarget` input/textarea/select/contenteditable guard). Layer 2 component: `tests/component/AppShell.test.tsx` (restores from seeded storage, persists on change, Cmd/Ctrl+\ toggles but not while typing, rapid double-toggle, throwing-storage resilience, Agents is the only enabled destination — the four deferred items are non-links with `aria-disabled`, active-nav derivation, labeled primary `<nav>`, and the **G3 hydration** instrument: a real `renderToString`→`hydrateRoot` reconciliation asserting `recoverable` is empty, confirmed falsifiable). Supporting test infra: `tests/helpers/hydrate.tsx` (hydration instrument), a rewritten `tests/setup.ts` (standing `console.error` trap with an `allowConsoleError` opt-in), and `tests/unit/no-suppress-hydration.test.ts` (grep guard that no source suppresses hydration warnings). `coverage_gate` **PASS** — 100% stmts/lines/funcs on every S-106 module; `make validate` green (Python 436 + panel **331 passed / 19 Docker-gated skips**). Known non-blocking gap: AC-106.6 (1024px, no horizontal scroll) has no automated evidence — jsdom computes no geometry, so it rests on token-driven CSS + manual comparison; S-114's E2E suite did **not** add a viewport-geometry assertion, so this stays a manual/visual check pending a future viewport scenario (verifier drift D4 / test-plan G7).
- **Agents-dashboard test surface (issue #120 / S-107).** The dashboard adds a Layer 1 + Layer 2 (component) + Layer 2.5 surface, the non-integration part run without a browser via jsdom (69 tests across four suites, plus the Docker-gated integration suite). Layer 1 unit: `tests/unit/dashboard.test.ts` (the FR11a/CT-1 invariant — a stale `running` row counts as `timed_out` in `breakdown`/`legend` and reads `timed_out` in `recentRuns`/`lastRun`; CT-3 no restated thresholds; the `matchesQuery`/`filterAgentSummaries` case-insensitive name+slug predicate with literal-metacharacter/`[`-cannot-throw cases EC-25; empty-fleet and unknown-future-status EC-23 handling), `tests/unit/density-state.test.ts` (the closed `{dense,cards,ledger}` vocabulary; unknown/corrupt/empty/absent value → `dense` default; throwing `getItem`/`setItem` swallowed CT-4/CT-5; round-trip), and `tests/unit/summary-view.test.ts` (the shared presentation helper). Layer 2 component: `tests/component/dashboard.test.tsx` (the three §5.1 variants render from one payload; density toggle persists and reconciles after mount; the transient filter is never persisted; ledger keyboard navigation; Invoke rendered disabled until the S-113 route lands). Layer 2.5 (Docker-gated, **ran live** for this delivery): `tests/integration/dashboard-query.test.ts` (`getDashboardData` two-read shape — never N+1, CT-7 — and the >1000-run paged read proving counts stay true beyond `max_rows=1000`, CT-8). `coverage_gate` **PASS** — 100% stmts/lines/funcs on `lib/domain/dashboard.ts` and `lib/ui/density-state.ts`; `make validate` green (Python 436 + panel **428 passed / 24 skipped** locally, the 24 skips being the Docker-gated integration projects incl. `dashboard-query`). Verifier audit: High fidelity, 8/8 ACs, all drift Minor/Intended (D1 time-range chips → `product-engineer`, D2 1024px geometry → S-114, D3 slug-link addressed).
- **Run-history test surface (issue #121 / S-108).** The run-history screen adds a Layer 1 + Layer 2 (component) + Layer 2.5 surface, the non-integration part run without a browser via jsdom (unit 28, component 17+7, integration 5). Layer 1 unit: `tests/unit/run-row.test.ts` (the FR11a invariant — a stale `running` row presents `timed_out` in the row AND is not counted a success in the header rate; the clock-injected `presentDuration` — live run → `running · Xm`, finished → `Xm XXs` via `duration_ms` or `finished − started`, never-started → `—`, never `NaN`; `presentStartedRelative` `started_at → created_at` fallback; `outcomeLabel` all outcomes incl. `not_applicable` → `N/A`; `lowerMedian` p50 returns a real observed duration on even counts; `buildAgentHeader` success rate / run count / params count; `shouldRunHistory404` unknown-slug and disabled-agent both true, enabled false). Layer 2 component: `tests/component/run-history.test.tsx` (semantic `<table>` markup — `role="table"`/`row`/`columnheader` rather than a div grid; the §5.2 fields render; newest-first order preserved from the query; Invoke disabled until S-113) and `tests/component/run-history-edge.test.tsx` (empty history, a run with no repository, a run with no outcome, a running row). Layer 2.5 (Docker-gated, **ran live** for this delivery): `tests/integration/runs-by-agent.test.ts` (`getAllRunsByAgentSlug` newest-first + paged read staying true beyond `max_rows`; `getStepProgressForRuns` one grouped read never N+1 and the `done=non-pending/total` fold). `coverage_gate` **PASS**. `make validate` green both branches (Python 436 + panel **510 passed / 4 skipped**). Verifier audit: High fidelity, all drift Minor/Intended (incl. D4 — the DESIGN §8.2 outcome-tag set does not list `N/A`, routed to `product-engineer` for DESIGN write-back, not made here). Deferred, non-blocking: the 1024px pixel-geometry check (jsdom computes no geometry) rests on token-driven CSS — S-114's E2E suite did **not** add a viewport-geometry assertion, so this remains a manual/visual check pending a future viewport scenario (routed to `product-engineer`).
- **E2E suite + CI Layer 2.5 gating (issues #127 + #134 / S-114).** This is a tests + CI story — no product code, no schema/data/API change (migration is a documented N/A opt-out). The **Playwright E2E suite** lives at `panel/tests/e2e/`: `playwright.config.ts` (retries 0, one worker, headless in CI, `webServer` runs `next dev`), `global-setup.ts` (resets/verifies the local Supabase stack, applies `supabase/migrations/` + `seed.sql`, asserts the seeded catalog, applies the local-only `service_role` SELECT grant per §7, warms Realtime, and stubs the AgentCore HTTP boundary via `AWS_ENDPOINT_URL_BEDROCK_AGENTCORE`), fixtures, and **7 scenarios + edge cases** across `invoke.spec.ts` / `live-tail.spec.ts` / `stale-and-artifact.spec.ts` / `density.spec.ts` / `edge-cases.spec.ts`. Scenario→AC traceability: 1→AC12 (invoke), 2→AC6 (autoscroll/pause-resume), 3→SD6 (backfill→subscribe→dedupe), 4→AC10 (reaper-paused stale read), 5→AC13 (density persistence), 6→AC9 (run history), 7→AC14 (artifact links on a failed run) — **all 10 E2E tests pass live**, including a cold-start run. `panel/tests/integration/e2e-fixture.smoke.test.ts` is a Layer 2.5 smoke over the seed fixture. **The standing G2 obligation is closed here:** `panel/tests/integration/db.ts` makes `REQUIRE_LOCAL_DB=1` turn a Docker-gated Layer 2.5 skip into a hard failure (enforced in `probeLocalDb`, uniform across all thirteen suites), and `.github/workflows/ci.yml`'s panel job boots the stack (`supabase start` + `db reset`), exports env, sets `REQUIRE_LOCAL_DB=1`, and runs the gated Layer 2.5 suites (`test:coverage`) + E2E (`playwright install --with-deps chromium` then `test:e2e`) — so a Layer 2.5 skip can no longer pass CI vacuously (CI runtime +~2–4 min for stack + browser provisioning). Both gate directions were demonstrated RED then reverted (status-parity on an inverted comparison; rls-deny-all on a permissive anon policy) — evidence in `workstream/s114-negative-demos.md`. `TESTING.md` was updated by the developer with the CI-vs-local skip policy, the scenario-to-AC table, and the E2E / Layer 2.5 package rows (not duplicated here). `coverage_gate` **PASS**, verifier audit **High fidelity 9/9 ACs** (highest drift Minor, all Intended). Suite counts at delivery: `make validate` exits 0 — Python **452 passed**; panel **774 passed / 4 skipped** (the 4 skips are the `RUN_BUNDLE_SECRET_TEST`-gated bundle tests); E2E **10 passed**. Two current-state findings recorded and flagged to `product-engineer` (not fixed here): (1) `next build` fails on the S-110 SSE route's non-route export `parseAfterSeq` — a pre-existing latent defect that does not affect the E2E run because it uses `next dev`; (2) the §7 local `service_role` grant asymmetry is reproduced in `global-setup.ts` (service_role only, RLS deny-all preserved).
- **Login-screen test surface (issue #158 / S-119).** The login screen adds a Layer 1 + Layer 2 (component) + Layer 2.5 + E2E surface — no product-code change outside the new screen; no schema/data/API change (auth state lives in Supabase `auth.*` + cookies, migration is a documented N/A opt-out). Layer 1 unit: `tests/unit/login-action.test.ts` (16 — the `resolveSignIn` core: missing-field validation; **anti-enumeration** — unknown-email and wrong-password return byte-identical results via `classifySignInError`; redirect sanitized **before use** and preserved across success/failure; a thrown transport error maps to `AUTH_SERVICE_UNAVAILABLE`; the password never appears in the returned failure shape), reusing the S-116 `tests/unit/auth-{errors,redirect}.test.ts` policy suites already present. Layer 2 component: `tests/component/LoginForm.test.tsx` (8 — pending/disabled double-submit guard via `useFormStatus`; the `role="alert"` generic message on failure; password field cleared (remounted) after a failed attempt; the `SHOW`/`HIDE` toggle defaults masked, flips `aria-pressed`, is `type="button"` so it never submits; the dead "Forgot password?" is a non-activatable `<span aria-disabled>`, not an `<a>`; label association). Layer 2.5 (Docker-gated, **ran live** for this delivery): `tests/integration/auth-login.test.ts` (4 — the sign-in round-trip against the real local Supabase stack, including that **RLS deny-all is unchanged** by authenticating a user, spec §14.3). E2E: `tests/e2e/auth.spec.ts` (the unauthenticated scenarios — protected-route → `/login` redirect AC1, valid sign-in → sanitized target AC3, tampered off-origin redirect → `/` AC4, generic `role="alert"` error AC5, SHOW toggle AC13, `redirect=/login` no-loop edge), the `tests/e2e/auth.setup.ts` **setup project** (signs the test operator in through the real `/login` flow once and saves `storageState` so the existing protected-route specs run authenticated under the now-active S-117 gate), with `global-setup.ts` provisioning the operator user via the Supabase admin API against the local stack (accounts are dashboard-provisioned in production, never seeded — mirroring the migration opt-out). `coverage_gate` **PASS**; verifier audit **High fidelity** (highest drift **Minor**, all Intended). Verification (reported by `qa-engineer`, confirmed consistent with the audited code): `make validate` green both branches (Python **452** + panel **915 / 4 gated skips**), `next build` green with `/login` dynamic, **E2E 17 passed** incl. the 7 auth scenarios, Layer 2.5 `auth-login` ran live. One non-blocking gap (verifier D2, routed to `qa-engineer`): the already-authenticated-visit → `/` redirect is verified by inspection, not yet by a dedicated automated test.
- **Logout test surface (issue #159 / S-120).** The sign-out path adds a Layer 1 + Layer 2 (component) + E2E surface — no schema/data/API change (auth state lives in Supabase `auth.*` + cookies, migration is a documented N/A opt-out). Layer 1 unit: `tests/unit/logout-route.test.ts` (the `POST` handler — best-effort/idempotent `signOut`, always `302 → /login`, **never 500s** on a thrown/absent-session auth error, no `GET` export so `GET` is not a logout) and `tests/unit/auth-route-policy.test.ts` (the S-116 `classifyRoute` reclassification — **exact** `/api/auth/logout` → `public`, while `/api/auth/logout/extra`, `/api/auth/whoami`, and other `/api/**` stay `api`; `/login` still `public`; unknown/empty paths fail-closed to `ui`). Layer 2 component: `tests/component/LogOutItem.test.tsx` (renders a `<form method="post" action="/api/auth/logout">` with a `type="submit"` button, accessible name "Log out" preserved collapsed/expanded, no client handler) and `tests/component/panel-layout-wiring.test.tsx` (the `authenticated` prop gates the sidebar Log out item — present when authenticated, absent when not; threaded through `AppShell → Sidebar`). E2E: `tests/e2e/auth.spec.ts` gains the logout scenarios (authenticated operator clicks Log out → `POST` → `302 /login` → protected route now redirects to `/login`). `coverage_gate` **PASS**; verifier audit **High fidelity 9/9 ACs** (highest drift Minor). Verification (reported by `qa-engineer`, consistent with the audited code): `make validate` exits 0 — Python **452 passed**; panel **898 passed / 49 skipped**; lint/format/typecheck/audit green; `next build` green with `/api/auth/logout` dynamic; E2E logout scenarios pass.
- **Live-tail 401 test surface (issue #160 / S-121).** The "stop the infinite 401 reconnect" fix adds a Layer 1 (unit) + Layer 2 (component) surface, run without a browser via jsdom (unit 6, component +2). Layer 1 unit: `tests/unit/use-run-stream-auth.test.ts` (6 — an `onerror` on an attempt that **never opened** with `readyState === CLOSED` sets `authStopped` and does **not** reconnect or schedule a timer (spec OQ3); a drop **after** `onopen` (or after the first `event`/`run` frame marked the attempt opened) keeps the S-110 reconnect with the highest rendered `seq` as `after_seq` and `SeqCursor` dedupe intact; a terminal `closed` frame is still not an auth stop). Layer 2 component: +2 in `tests/component/live-log-viewer.test.tsx` (on `authStopped` the viewer renders the `role="alert"` session-expired notice and drops the live-tail control; a normally-connected viewer shows neither). No schema/data/API change (migration is a documented N/A opt-out); `lib/sse/relay.ts` sequencing untouched. Verification (reported by `qa-engineer`, consistent with the audited code): `make validate` green (Python **452 passed** + panel **906 passed / 49 gated skips**), `stream-e2e` ran live and passed, `next build` green, `coverage_gate` **PASS**, verifier audit **High fidelity 6/6**.

## 12. Code Quality & Standards

The JS/TS quality toolchain is now real (Phase 2 has begun — issue #114 / S-101). A root
`package.json` exists (`packageManager: pnpm@10.11.0`, `engines.node >=22`) and a
`pnpm-workspace.yaml` declares the workspace members (`panel`, `agents/dependency-update/agentcore/cdk`).
The `panel` package uses **ESLint 9** (`next/core-web-vitals` + `next/typescript`, plus an SD2
restricted-import rule that forbids client-side import of `lib/supabase/server`), **Prettier 3**,
and **TypeScript 5.7 strict**. The canonical `package.json` scripts are wired on both the root
(delegating to `panel` via `pnpm --filter`) and the `panel` package itself: `lint`, `lint:fix`,
`format`, `format:check`, `typecheck`, `test`, `test:unit`, `test:integration`, `test:e2e`,
`test:coverage`, `audit`, `validate`. The `panel` `validate` aggregate runs
lint + format:check + typecheck + test + audit, and is reached from the repo-root `make validate`
JS/TS branch (F7 closed — the panel is inside the aggregate gate). The Python agent keeps its own
`ruff`/`mypy` toolchain (§11, `TESTING.md`); the two branches run independently under
`make validate` and both must pass.

The panel's server-only Supabase read boundary (SD2) is enforced by two layers as of issue #117 /
S-104: the **hard** guard is `import "server-only"` at the top of `lib/supabase/server.ts` (pulling
that module into a client bundle fails `next build` with an RSC error that no `eslint-disable` can
suppress), and the ESLint `no-restricted-imports` rule is a **fast lint-time hint** scoped to the
client-component trees — it was rescoped from tree-wide because the prior scope
flagged legitimate Server Components (exactly where SD2 reads belong) during `next build`. As of
issue #119 / S-106 the rule's glob was **extended from `components/**` to also cover `app/**`** (the
S-104 audit D1 hardening), with the App Router server entrypoints
(`page`/`layout`/`route`/`template`/`default`/`error`/`loading`/`not-found`) explicitly excluded so
the rule still never fires where SD2 reads legitimately live — it now catches a stray
`lib/supabase/server` import from a client component *nested under a route directory*, which the
`components/**`-only scope missed. The
guarantee that matters — the service-role key never enters the browser — is carried by
`server-only`, so the extension is strictly stronger, not weaker; `tests/unit/eslint-server-import.test.ts`
proves the rule still fires. Separately, route-segment config
(`dynamic`/`revalidate`/`fetchCache`) **must be declared inline** in each data route: Next.js does
not honor those exports when re-exported from another module (it silently falls back to defaults), so
`lib/supabase/route-config.ts` holds the canonical values as documentation and Wave 3 data screens
copy them inline.

The panel's **Nocturne token discipline** is a mechanical gate as of issue #118 / S-105 (the
tokens + CSS-Modules standard itself traces to DESIGN.md §11.1 / S-101 — S-105 mechanizes it, it is
not a new decision). Every color, font, spacing-scale step, radius, and shadow value **must** come
from a CSS custom property defined in `styles/tokens.css` (the single literal home, transcribed from
DESIGN §2); each primitive carries a token-only `.module.css`. `tests/unit/token-discipline.test.ts`
enforces this by rejecting any color hex literal and any `font-family:` literal under `components/**`
and `styles/globals.css`. One deliberate carve-out: bare dimensional `px` is **not** rejected — the
Nocturne prototype fixes exact pixel dimensions that are not part of the six-step `--space-*` scale
(grid track sizes, dot/knob diameters, control min-heights, 1–3px radii); a blanket no-`px` rule
would false-positive on faithful reproduction of the visual contract, so dimensional `px` is allowed
and remains a review point (documented in the test header and `panel/README.md`). Icons come from
`@phosphor-icons/react/ssr` on `currentColor`, imported by semantic role from `components/icons.tsx`
(DESIGN §10) — no Unicode glyph stand-ins (the `✓ ✕ ⧗` in `formatStatusLegendCompact` are DESIGN
§7.3 *content*, not icon stand-ins). `color-mix(in srgb, …)` is used throughout, so the panel
targets evergreen engines only (Chrome/Edge 111+, Safari 16.2+, Firefox 113+ — DESIGN §11.1) and
ships no fallback for older engines.

## 13. Deployment & DevOps

- **Front-end:** Fly.io. A prior app existed (`dt-agent-fleet-control-plane`, torn down in the reset — see `RESET-PLAN.md` Phase 2). The `panel` Next.js package exists in the repo as a scaffold (issue #114 / S-101). As of issue #128 / S-115 its **production deploy configuration and release gate are committed, but the panel is still NOT deployed** — the live deploy is operator-gated per the runbook. What shipped (committable half): `panel/Dockerfile` + `panel/.dockerignore` (a Next.js `standalone` production image built from the monorepo-root context, no secrets baked in, verified to build and boot locally), `panel/next.config.ts` `output: "standalone"`, `panel/fly.toml` (**private-only by construction** — no `[http_service]`/public ports, SR2/D16 banner, app `dt-agent-fleet-panel`, region `iad`, secrets set via `fly secrets` never committed), and the privacy release gate `scripts/verify-fly-private.sh` + `panel/scripts/fly-privacy-check.mjs` (§6, SR2 — fail-closed, unit-tested). The **live half is a documented operator procedure, not yet executed** — `fly deploy`, the AWS OIDC IdP + IAM role/trust-policy, `fly secrets`, the live OIDC socket probe (the OQ1 closer, §5), the `DurationSeconds: 900 ≤ MaxSessionDuration` check, and the live end-to-end invocation all require real, hard-to-reverse cloud actions and are gated on explicit operator execution in [`runbooks/panel-deployment.md`](runbooks/panel-deployment.md) (with an evidence log and rollback; no secret material recorded). The `next build` that the production image requires is now green — S-115 moved the S-110 SSE route's non-route `parseAfterSeq` export into `panel/lib/sse/cursor.ts` (behavior unchanged), resolving the latent defect flagged at S-114. CI (`.github/workflows/ci.yml`) gained a dedicated privacy-gate parser test step and a `shellcheck` step for the wrapper. **As of issue #161 / S-122 the privacy release gate is REPLACED by the auth release gate** ([ADR-007](adr/ADR-007-auth-release-gate-replaces-privacy-gate.md), §6): the auth wave reverses D16 (the panel now requires login), so `scripts/verify-fly-private.sh` + `panel/scripts/fly-privacy-check.mjs` are removed and `scripts/verify-panel-auth.sh` + `panel/scripts/panel-auth-check.mjs` take their place — a fail-closed gate that probes the **deployed** app (env-name presence, protected UI → 302 /login, SSE → 401, signUp rejected) at a hostname argument, and the CI steps now run the auth parser tests + `shellcheck` the auth wrapper. The rollout is now two operator phases (`runbooks/panel-deployment.md`): **Phase A** deploys the auth build and verifies the gate over the **private** network (app stays private — `fly.toml` still declares no `[http_service]`/public IP); **Phase B (S-123, separate PR)** allocates a public IP + enables the public HTTPS service, only after Phase A passes, behind an explicit operator-confirmation gate and reversible in one command (`fly ips release`).
- **Agents:** AWS Bedrock AgentCore. The prior deploy used a CloudFormation stack (`AgentCore-depupdater-default`) managed by the AgentCore CLI — also torn down in the reset. Re-deployment is Phase 1.
- **Supporting AWS infrastructure (IAM, OIDC provider, orchestration, config table):** the prior iteration used CDK (`AgentFleetIamStack`, `AgentFleetOrchestrationStack`, `AgentFleetDataStack` on DynamoDB). All destroyed in the reset. v2 replaces the DynamoDB config table with the Supabase tables described in §7 — no equivalent CDK stack is planned yet for v2; will be defined when the IAM setup from §5 is implemented (OIDC provider + role).
- **Known note:** `cdk destroy` does not work under Node.js v26 with `aws-cdk-lib@2.266.0` (incompatibility documented in `RESET-PLAN.md`). If CDK is reintroduced in v2, validate the `aws-cdk-lib` version against the Node version in use before depending on `cdk destroy`/`cdk deploy`.

## 14. Monitoring, Logging & Observability

- **CloudWatch** remains as infrastructure telemetry (container startup, crashes not captured by the SDK), outside the panel's functional scope (see §3). It also serves as a fallback destination when the agent SDK cannot reach the Supabase API — payloads are dumped to stderr, which lands in CloudWatch.
- **Supabase Realtime** on `run_events` and `runs` is the business observability mechanism — live log tail without polling (Phase 2 enables the UI for this; Phase 1 writes the data).
- **Captured log level** by the SDK's `logging.Handler` is configurable via `AGENT_LOG_LEVEL` (default `INFO`), first mitigation lever for R5 if an agent proves too verbose.
- **Reaper observability (issue #94).** The `pg_cron` job is its own telemetry surface: `cron.job` shows the registered `reap-stale-runs` entry and `cron.job_run_details` shows per-tick success/failure — the check to run when runs stop being reaped. Every materialized transition also writes one `run_events` row carrying `data.reaped_by` and `data.reason`, which is the only record of *why* a run ended when the agent itself never reported (product-context success metric 3). No alerting is wired on either surface in v1; detection is manual, per [`runbooks/issue-94-reaper-verification.md`](runbooks/issue-94-reaper-verification.md).

## 15. Performance & Scalability

- Indexes on `runs` are designed for the most frequent listing queries: by agent and by repo, both ordered by `created_at desc` (§7).
- The SDK write buffer (50 events / 2 seconds) is the only active performance lever in v1 for log write volume; see R5 for the analysis of why it is not adjusted preemptively.
- No formalized latency or throughput targets — the expected scale in v1 is few agents with non-continuous executions (see `product-context.md` §11, Assumptions).

## 16. Dependency Management

- **Agent — reporting SDK:** zero external dependencies — Python stdlib only (explicit decision, D13). [`agent_reporter.py`](reference/agent_reporter.py) is copied byte-identical into the agent repo; because that verbatim copy trips one mypy check (`exit-return`) on mypy 2.3.1, a per-module override in `pyproject.toml` suppresses that single error code for `agent_reporter` only — the file is not modified and the rest of the codebase keeps full strictness (ADR-001).
- **Agent — runtime (`dependency-update`):** pinned in `pyproject.toml`, `requires-python >=3.13`. Committed runtime dependencies: `bedrock-agentcore` (runtime harness), **`strands-agents` (the LLM fix agent, now activated by the issue #75 escape hatch — a real runtime dependency, no longer "outside scope")**, `boto3`, `requests`, `PyJWT`, `cryptography`. The Bedrock model is selected via `MODEL_ID` (default `us.anthropic.claude-sonnet-4-6`). Dev/quality tooling (also pinned, `[dev]` extra): `pytest`, `pytest-mock`, `pytest-cov`, `ruff`, `mypy`, `pip-audit`. The `audit` gate runs `pip-audit .` scoped to declared runtime deps, not the ambient venv.
- **Front-end (`panel`):** the render/validation dependency tree is now pinned (Phase 2 has begun — issue #114 / S-101). Runtime deps in `panel/package.json`: **`next` 15.5.25** and **`eslint-config-next` 15.5.25** (the 15.5 backport line — pinned off the spec's 15.5.4 because 15.5.4 carries a critical RCE advisory; the version write-back to the spec is routed to `product-engineer`, not made here), **`react`/`react-dom` 19.1.1**, **`ajv` 8.17.1** + **`ajv-formats` 3.0.1** (for `params_schema` validation in later stories), and **`@phosphor-icons/react` 2.1.10** (DESIGN §10 icon set — actively used as of issue #118 / S-105 via `components/icons.tsx`, imported by semantic role from `@phosphor-icons/react/ssr`). Dev/quality tooling (also pinned): **`vitest` 3.2.4** (config-level `projects` API) + **`@vitest/coverage-v8` 3.2.4**, **`@testing-library/react`/`jest-dom`**, **`jsdom`**, **`playwright`/`@playwright/test` 1.56.0**, **`eslint` 9.18.0**, **`prettier` 3.4.2**, **`typescript` 5.7.3**. Root `package.json` `pnpm.overrides` pin **`postcss>=8.5.18`** and **`sharp>=0.34.4`** transitively (audit-driven). One residual **moderate** `ajv` advisory sits below the `--audit-level=high` gate and does not fail `audit`. As of issue #117 / S-104 the Supabase client is no longer deferred: **`@supabase/supabase-js` is pinned `2.114.0`** in `panel/package.json` (spec §16 names 2.58.0 — the newer pin was re-confirmed current and audit-clean before pinning, the same "re-confirm-then-pin" precedent set for `next` in S-101; the 2.58.0→2.114.0 spec write-back is routed to `product-engineer`, not made here) and **`server-only` `0.0.1`** is added as the build-time client-bundle guard for `lib/supabase/server.ts` (§9). As of issue #124 / S-111 the `@aws-sdk/*` clients are no longer deferred — the invocation path shipped and four packages are pinned in `panel/package.json`: **`@aws-sdk/client-sts`**, **`@aws-sdk/client-bedrock-agentcore`**, and **`@aws-sdk/credential-providers`** at **`3.1126.0`**, and **`@aws-sdk/types`** at **`3.974.5`**. `@aws-sdk/types` tracks an independent, slower release cadence, so a byte-identical minor across all four is not achievable; the substantive invariant that is actually met is a single deduplicated **`@smithy/core@3.33.3`** in `pnpm-lock.yaml`. No new advisory: the AWS SDK additions leave the `--audit-level=high` gate green (the residual `ajv` moderate remains the only sub-gate advisory).

## 17. Development Workflow

- **Active branch for this reset:** `chore/project-reset`, on `origin/main` without fast-forward pending merge as documented in `RESET-PLAN.md` Phase 3.
- **Branch convention for new work:** `story/*` for user stories, `issue/*` for individual issues (naming observed in the repo's commit history, e.g., `issue/62-*`).
- **Commits:** Conventional Commits (active repository rule, see git-guard notice). The history already follows this pattern (`fix(agent): ...`, `docs: ...`, `chore(workstream): ...`).
- **PRs:** require `--body-file`, never inline `--body`, for GitHub issues and PRs (active repository rule).
- **No agent may push or merge directly to `main`.**

## 18. Known Constraints & Trade-offs

Risks explicitly accepted in v1, with their declared exit path (full detail in the technical specification):

| Risk | Accepted trade-off | Future exit |
|---|---|---|
| R1 — No authentication **(RESOLVED, auth wave S-116…S-122)** | ~~The panel can invoke agents without login~~ | **Resolved:** the panel now requires a Supabase password login (`middleware.ts` fail-closed gate, `/login`, POST logout, SSE `401`), and the boundary is mechanically asserted by the auth release gate (`scripts/verify-panel-auth.sh` + `panel/scripts/panel-auth-check.mjs`, [ADR-007](adr/ADR-007-auth-release-gate-replaces-privacy-gate.md)). The former "keep the app private / shared-secret header" mitigation is superseded for the login boundary; the private-app deploy state remains until the separate go-public step (S-123). |
| R8 — Public login endpoint invites credential-stuffing / brute force (new; auth wave) | Account compromise → agent invocation | Rely on Supabase built-in rate limits for v1 (OQ4 accepted); strong operator password; the signup-disabled release blocker (auth gate check 4) prevents self-registration; revisit with lockout/CAPTCHA if abuse appears |
| R2 — Agent uses Supabase `service role key` | Full database access from the agent container. Key stored in Secrets Manager, not in env vars (D15). | Dedicated Postgres role with scoped grants + signed JWT per run |
| R3 — `run_events` growth | Largest table by two orders of magnitude, no active retention policy | Collapse to Storage + purge at 90 days |
| R4 — `params_schema` without strong cross-validation | Drift between the schema in the database and what the agent expects is only detected at runtime | Agent validates its own payload at startup and fails with `INVALID_PARAMS` |
| R5 — Write volume within a verbose execution | No preventive mitigation | Raise minimum captured level, logger allowlist, truncate per step, or increase batch — "evaluate later" is the chosen option today |
| R6 — Drift between local and Fly environments (SSO permissions vs. scoped OIDC role) | Something that works locally may fail on Fly due to a missing permission | Assume the same role locally with a profile pointing to the Fly `role_arn` |
| R7 — Test runs against the production database | Local development invokes real AgentCore and can write to the configured Supabase | Second Supabase project dedicated to development |

### Open defects discovered during reaper verification (issue #94)

The `pg_cron` reaper was scheduled and its stale-run detection verified in issue #94 (see
[`runbooks/issue-94-reaper-verification.md`](runbooks/issue-94-reaper-verification.md)). The reaper
behaved correctly in every observed case, but the verification surfaced four defects elsewhere in
the stack. Each was tracked as its own issue; the **Status** column below records its current
resolution state:

| Issue | Defect | Impact | Status |
|---|---|---|---|
| [#97](https://github.com/llipe/dev-tasks-agent-fleet/issues/97) | `unwrap_payload()` strips exactly one `prompt` wrapper, but `agentcore` CLI ≥0.28.0 wraps the prompt argument itself — the documented pre-wrapped form arrives double-wrapped and dies with a generic `INVALID_PARAMS` | Any invocation using the README / #77-runbook examples fails; the error gives no hint of the real cause | **Resolved (PR #102):** `unwrap_payload()` now strips nested lone-`prompt` wrappers in a loop (bounded by `_MAX_UNWRAP_DEPTH=16`), and a still-wrapper-only payload emits a distinct "appears double-wrapped" diagnostic while `error_code` stays `INVALID_PARAMS`. See §8 and [ADR-005](adr/ADR-005-repeated-prompt-unwrap-and-diagnostic.md). |
| [#98](https://github.com/llipe/dev-tasks-agent-fleet/issues/98) | A run died during the `validate` step without reporting terminal status. The entrypoint yields only its final result, so a run streams nothing for its whole duration against `idleRuntimeSessionTimeout: 300`; `TEST_TIMEOUT` defaults to 600 s, twice that. Root cause unconfirmed (idle-timeout vs. OOM both fit) | If runs cannot survive ~5 min, `maxLifetime: 3600` is moot and no long `llm_fix` run completes. **Blocks issue #94 AC5** | **Resolved (code) / live-verify pending (PR #103):** root cause confirmed as output-idle reclamation from CloudWatch (clean silence on run `f63ac9f3-…`, no OOM signature). `validate`/`llm_fix` now live-yield heartbeat chunks (`heartbeat.py`), `idleRuntimeSessionTimeout` raised 300 → 900, and the four timeout clocks are enforced consistent by `config.assert_clock_invariant()`. Best-effort SIGTERM backstop added; SIGKILL/OOM stays reaper-only. See §8. AC2 (>5 min validate) and AC3 (>20 min llm_fix) require a runtime redeploy to verify live. |
| [#99](https://github.com/llipe/dev-tasks-agent-fleet/issues/99) | `reap_stale_runs()` transitions `runs` and writes the explanatory event but never closes open `run_steps` — unlike the agent's own failure path (§8), which closes them as `failed` | Every reaped run leaves an orphan step pinned `running`; the Phase 2 Run Detail panel (DESIGN.md §5.3) would render a perpetually pulsing step inside a terminal run | **Resolved:** `reap_stale_runs()` now closes open `run_steps` (`status='failed'`, `finished_at=now()`, attributing `error_message`) on **both** branches (`timed_out` and `failed_to_start`), mirroring the agent path (§8). Reuses the existing `step_status` enum value `failed` (no migration); leaves already-terminal steps untouched; safe 0-row no-op when a run has no steps. See §7/§8. |
| [#100](https://github.com/llipe/dev-tasks-agent-fleet/issues/100) | Per D1 the control plane inserts the `queued` `runs` row; the agent SDK only PATCHes it. A direct `agentcore invoke` therefore leaves the run invisible — PostgREST returns HTTP 200 on a zero-match UPDATE | Runs appear to vanish with no error anywhere; documented contract omits the insert requirement | **Resolved:** the insert-then-invoke prerequisite is now documented (README §Invocation, reaper-runbook §4.0), and `RunReporter.start()` issues its `running` PATCH with `Prefer: count=exact`, reads `Content-Range`, and on a **confirmed** zero-row match logs a loud stderr warning naming #100 before continuing (an unknown count does **not** warn). No schema/migration change; small behavioral addition under the existing D1/D15 reporting-contract context, no new enforceable rule. See §8 (Behavioral properties → *Zero-row `start()`*). |

**Verified-correct reaper behaviour worth recording** (from the same exercise, real run
`f63ac9f3-14b0-4157-9484-f2f6b062f846`): the reaper fired 12.3 s after the 3720 s threshold
(`max_runtime 3600 + grace 120`) — inside one cron tick, never early; its explanatory event took
`seq = max(seq)+1` without colliding with `uq_run_events_seq`; and `v_runs.effective_status`
reported `running` pre-threshold then agreed with `timed_out` after materialization, confirming the
two-layer design in §3.

**Accepted consequence of the D8 threshold choice.** Because `max_runtime_seconds` mirrors
AgentCore's `maxLifetime` (3600) plus `grace_seconds` (120), a container that dies early still reads
`running` until the 3720 s boundary. The run above died around 19:36 and was marked `timed_out` at
20:37 — a ~61-minute window of plausible-but-stale state. This is the accepted tradeoff, not a
reaper failure; `last_heartbeat_at` (declared, unused in v1) is the lever if it ever needs
tightening.

**Residual verification, not a defect.** Issue #94 closed with **5 of 7 acceptance criteria
verified** (AC1, AC2, AC3, AC4, AC7). Three checks remain and are carried by
[#101](https://github.com/llipe/dev-tasks-agent-fleet/issues/101): AC5 (a healthy long run is not
reaped early, plus a *valid* cold-start measurement), AC6 (the SDK's CloudWatch fallback when
PostgREST is unreachable), and the `queued` → `failed_to_start` read-time half of AC4 — `v_runs` has
two independent branches and only the `running` one was observed. Two consequences to respect until
#101 closes: the reaper's **must-not-reap** direction rests on one incidental observation rather than
a deliberate check, and the earlier cold-start figure of **185.7 s is invalid** (it includes human
delay between the row INSERT and the `agentcore invoke`; the agent's first log and `started_at` were
180 ms apart). It must not be cited as a measurement or used to justify a `grace_seconds` change.
`TESTING.md` (Database / reaper layer) ranks the corresponding structural test gaps.
